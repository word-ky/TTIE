# T047-A fixed marginal additive-lift capacity probe

REFERENCE_ORACLE_ONLY. Accepted main8a8d944d / T046 merge0081ad23. Exact T046 freeze2cdf5b8f, pairs93e6b6dc, prior preflightc7a75601, fixed100 splitb88c8347.
All100low-only zero-lift reconstructions <=1e-6 before any normal decode. Keep accepted T046 raw EV/gamma/gain fixed. Cache immutable preclamp common-gain pixels once; insert physical RGB-shared b per active quadrant immediately after gain, retain the existing identity-contrast arithmetic, clamp and hard-gate compositing. b=0 initialization, projection[-.2,.2], inactive b=0. Fresh Adam([b],lr=.01), exactly500updates, one start; retain501states and earliest strict full RGB-MSE minimum. Float32 renderer, float64 MSE; CUDA GPU1, TF32off, threads1.
Freeze all100outputs before PSNR/SSIM. Independent replay verifies all50100lift states, earliest minima, unchanged legacy raws, hashes, reconstructs selected renderer from low/state independently, metrics/counts/distributions/verdict. Sole gate mean paired PSNR>=.5 AND median>=.25. Otherwise exact negative. No joint optimization, sweep, deployable changes, officialtest or nextoperator. Stop after one run and report.
