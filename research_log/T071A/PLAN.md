# T071-A exact held-out evaluation, no method changes

Reuse immutable T070A source aa4d920dff4b5b76751c24266e95ac9696d55d90 and Final-Ours manifest e7f129d931d27531e5f6a14cd72e8c94734c474c40c3c403959cc8f5764e5ab9. Prior200+10 full GPU replays exact; independent5600renders PASS, local7passed21.79s/remote7passed2warnings2.52s. No scientific file edits, environment change, optimizer settings or fallback.

Canonical provenance: author https://github.com/flyywh/CVPR-2020-Semi-Low-Light README LOL-v2 Google Drive link https://drive.google.com/file/d/1dzuLCk9_gE2bFF222n3-7GVUlSVHpMYC/view?usp=sharing; existing T022A original ZIP1046491030bytes SHA2569820d8b112438d94d1f5d4d25817eee618a8cf4bc63a65cd6b19f5a98c3faefa. Local ZIP central-directory-only inspection confirms Test/Low low00690..00789.png and Test/Normal normal00690..00789.png,100complete pairs. No reference payload has been opened. No cross-dataset access.

Reconcile file hashes with pre-reference boundary: pre-run manifest includes exact low SHA256 and reference CRC32/file size from ZIP central directory, pinned entire archive SHA256 and canonical source URL. Do not decompress/read reference members merely to calculate their SHA256 early. Add actual reference SHA256 only postfreeze, checking CRC32 against pre-run manifest. Archive opaque-byte hashing is provenance checking, not reference member access/decoding.

Stage runner: verify archive identity; pair all100 exactofficialnames; extract only low members into task-local staged low directory; write immutable pair manifest. Initialize exact FinalOurs, then use existing ReadScope restricting reads to staged lows while inference runs. Archive/references outside scope. For each image save raw float32 output.pt and selected state plus complete decision/trajectory hashes, timing. Freeze100rows with reference_reads0 and timestamp/hash. No sample exclusion/retry; failBLOCKED on any error. 100optimizer runs2700updates if complete,model fits0.

Postfreeze metrics: only after verifying freeze/table/file bindings record first-reference-access timestamp and read100normal members once. Reference decoding uses existing evaluator convention RGB uint8/255 rounded tofloat32 then float64, outputrawfloat32 castfloat64. PSNR -10log10 RGBMSE, range1, no crop/resize/quantization/clipping. Existing rgb_ssim11x11Gaussian sigma1.5 reflect populationRGBmean. Report meanPSNR,meanRGBSSIM,medianPSNR,selectedstep min/median/max/histogram and runtime. No baseline metrics or quality thresholds.

Independent verifier: regenerate officialpair metadata from ZIP centraldir, checklowhash/CRC/referenceSHA and completeids, frozen manifest/source/environment/outputs/decisions, timestamp ordering and exact inference data-read list. Recompute metrics with torch float64 MSE and independent separable-convolution SSIM, no optimizer/inference rerun; tolerance1e-11. Recompute aggregates and selection histogram. Source/output/metric disagreement => BLOCKED, no method changes.

Increment1 pair completeness/metric constants/aggregation tests. Increment2 remote staging then one100image inference and posthoc evaluation, independent verification and original inference API regression tests. All source committed/pushed before officialpixel processing. Final result regardless of metric strength. Stop before baselines/crossdatasets.
