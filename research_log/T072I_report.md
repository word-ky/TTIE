# T072-I — UHDLL_FULL_LOW_ONLY_DISPATCH_SEALED

The complete low-only execution contract is sealed for the canonical UHD-LL testing-set input cohort. The manifest contains 150 unique degraded JPEG inputs and the exact 450-job Cartesian product across Final Ours, RetinexFormer, and SNR-Aware. Every low was downloaded from the previously bound input-only metadata, decoded as RGB JPEG, and verified at native `3840×2160` geometry with `uint8` payload semantics. The byte index is `95c0a189b406c7dedb247ac2921f332658d76ced0db70a9c301e61d6c9d8d8fd` over sorted `name\tsha256` rows and totals `104767122` bytes.

The sealed manifest is `research_log/T072I/dispatch_manifest.json` with root SHA256 `a624d66cbb735bf1483720b1256b63e01d53b55da9f05eb0634d975214f4bd3e`. It anchors the accepted Final-Ours manifest, RetinexFormer source/checkpoint/config binding, and SNR-Aware source/checkpoint/config/parameter binding. Job outputs are deterministic and collision-free under `outputs/{method}/{stem}.{pt,npy}`. All execution options explicitly disable resize, crop, downsample, tiling, precision workarounds, target-specific normalization/tuning, and reference access.

`verify_dispatch.py` independently anchors the frozen binding literals and canonical low-metadata digest, recomputes every low-file SHA256 from bytes, decodes and checks every low geometry/mode, and verifies the 150×3 Cartesian job set. `test_dispatch.py` covers a complete pass plus missing job, extra job, duplicate input, altered binding, altered low hash, output collision, and injected reference path. The focused dispatch suite passes (`8 passed in 108.60s`); the inherited sealed T072-E harness passes (`11 passed in 52.76s`); Python compilation passes. The same verifier passes on the A6000 host against the uploaded input root with `UHDLL_FULL_LOW_ONLY_DISPATCH_SEALED`.

The same 150 low files were copied to `/media/wenchang/F/wjq/TTIE/shared/t072i/uhdll/input` on the A6000 host and independently rehashed there with the same count, byte total, and index SHA. No model was run, no GPU was used, no clean/GT/reference payload was accessed, and no metric was computed: `inference_runs=0`, `optimizer_runs=0`, `model_fits=0`, `reference_reads=0`, `metrics=0`.

This task stops at the dispatch contract. Native-4K baseline feasibility and the full 150-image inference remain separate later tasks gated by the accepted clean-A6000 requirement.
