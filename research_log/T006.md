# T006 source-trained exposure prototypes

## 2026-09-11T23:05:00Z — Receipt and predeclaration before training

T005 accepted at https://github.com/word-ky/TTIE/pull/5#issuecomment-5641532117 and squash-merged as0d2146257135c2b0c3568cbf579d6c78d2354114. Received OPEN T006 on main53c2203; branch codex/T006-learned-exposure-prototypes. Read full research task and project handoff. Existing38tests passed in13.588s (T006_baseline_tests.txt). Continue established incremental implementation and AutoDL workflow; no new team or method scope.

Before training: T006_manifest.json selects100 images from148 eligible unused images in the same200-image COCO cache. Exclude all48T004/T005 IDs, sort remaining numeric file IDs, require original min-side>=320, take next100, split first60source_train/next20source_calibration/last20evaluation. Exact requested sizes available, no split deviation. Metadata-only selection; no annotation/score inspection. IDs/dimensions/SHA256/source URLs/exclusion list persisted; images copied only to TTIE/shared/t006/images. Source-pool provenance remains incompletely known and sampling not representative. All splits disjoint and exclude previous tasks.

Reuse exact frozen ViT-B-32/laion2b_s34b_b79k checkpoint SHA2561bd3c7172de5b207ceac554f5ab5266166f3b9baccc9af5989bc801016d080ad, nine text prompts, RGB source shorter-side320 and five full/quadrant CLIP views (shorter224/center224), old six conditions, environment and normalization. Add only normalized image-embedding access to existing FrozenCLIP; reuse calibration/decisions/homogeneous metrics. No old ISP/adaptation changes, no new dependencies or CLIP finetuning.

Train exactly three512-dimensional prototype vectors (1536 scalar parameters), initialized by an exact clone of the existing normalized text ensemble prototypes in normal/dark/bright order. Normalize prototypes at every logit/score calculation. Precompute normalized frozen features for60source images x3conditions x5views=900 examples. Only clean->normal, gain.45->dark, gain1.55->bright labels from source_train are used. Choose deterministic FULL-BATCH cross-entropy over all900examples (300perclass, equal weighting), no minibatch/augmentation/sampling ambiguity. Fixed temperature.07, AdamW lr.005/weight_decay.0001,500updates, seed7, float32 CUDA; no early stopping or schedule. Record initial loss plus each post-update loss (501samples), gradient norms500, initial/final normalized prototype pairwise cosine and per-class alignment. Save source feature/label/ID receipt and learned raw/normalized vectors/hash before calibration/evaluation. Any numerical failure ends this run with preserved evidence; do not tune.

Freeze learned vectors and CLIP before scoring. Learned d_dark/d_bright are cosine differences to normal, WITHOUT training temperature. Calibrate each learned-score95th percentile and population std floor.01 from only20CLEAN source_calibration images/100views. For a fair same-split zero-shot reference, apply the exact same clean-only calibration rule to zero-shot scores on these same20images; prompts/prototypes/model remain unchanged. T006 does not require retaining prior-task thresholds (unlike T005); this baseline calibration choice is fixed before training/evaluation and will be explicit in the report. Persist model/tensors, config, source loss and both calibration constants before held-out scores.

Held-out audit:20images x6conditions x5views=600rows, plus100calibration rows. Preserve learned and zero-shot scores/decisions, clipping, and attach condition/view/region metadata only AFTER pixel-only scoring/gating. Reuse all/full/quadrant homogeneous FPR, tied-AUC, any/correctTPR, active-type precision, strict paired-score increase. No held-out clean reconstruction metrics or ISP adaptation.

Mixed localization uses only quadrant views of left_right and alternating quadrants:20images x2conditions x4views=160rows,80trulydark/80trulybright. Left_right TL/BL dark and TR/BR bright; quadrants TL/BR dark and TR/BL bright. Full views excluded, gradient condition excluded from binary localization. Report correct activation recall, wrong-type activation rate over ALL true-type views, conditional precision among active (null if none), and true-minus-other score margin mean/min/p25/median/p75/max plus per-row margins. Also provide left_right/quadrants separately and clean-quadrant FPR. Labels are offline audit only.

Literal learned-signal gate: clean all-viewFPR<=.15; eachhomogeneousAUC>=.80; eachhomogeneouscorrectTPR>=.40; combinedhomogeneousactiveprecision>=.85; mixedaggregate correctactivationrecall>=.40 for eachtrue type; mixedwrongactivationrate<=.15 for eachtrue type. Undefined active precision cannot pass. Subgroup/margin metrics do not alter thresholds or choose a model. Pass or fail, STOP after T006 audit: no T007, ISP adaptation, detector, CoOp, meta-learning or future modules.

Increments:1) embedding seam/prototype-only training+isolation tests;2) calibration/heldout/mixed audit+known-label tests;3) full regression, commit frozen config/manifest, run actual source training and fresh audit once;4) preserve/report all evidence and wait for research review.

## 2026-09-11T23:11:10Z — Implementation/test completion before training

Prototype/source-isolation increment:5tests passed in5.148s; affected existing CLIP5tests passed in2.412s. Mixed-localization/gate increment:3tests passed in.025s. Full46tests passed in7.561s, including all38previous regressions; git diff --check passed. Actual source training has not started and no T006 held-out score inspected. Exact commands: python -m unittest discover -s tests -p test_learned_prototypes.py -v; python -m unittest discover -s tests -p test_clip_signal.py -v; python -m unittest discover -s tests -p test_prototype_audit.py -v; python -m unittest discover -s tests -v. Source/protocol/manifest will be committed before deployment.
