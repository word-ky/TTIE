# T018-C execution log

## 2026-09-13 — source preparation

Research main 2bb78d69 / inbox e0b19979 issues the fixed direct-direction probe after accepting T018-B (PR 26, merge 4b396aea89345d60e4f7cb8d91b50bab3789b338). Branch: codex/T018C-direct-direction-oof. Read T018C_spec.md for the complete contract.

Reused T016-B saved features, T018-A training choices, T016-C fold bytes, and T018-B provenance/evaluation helpers. T016-C and T016-F fold files are byte-identical. Adapted the accepted CPU head lifecycle to the prescribed independent three-class heads. No image rendering, feature extraction, GPU work, or fresh qualification.

Fixed input: [center features, lower-minus-center, upper-minus-center], 84 dimensions. Training-row population standard deviation is clamped at 1e-12; float64 statistics are copied into float32 buffers. Seed 7 resets per head; 100 full-batch AdamW epochs, final epoch only. Each fit looks up targets only for its 96 training rows; inference receives only held-out feature tensors.

Precheck passed: 120 unique episode keys and aligned target ordinals/coordinates, nine 28-D candidates, exact schema, original five 32/8-image-ID and 96/24-row partitions. Full cross-artifact identity/corner matching and actual image-ID-to-row checks occur after OOF freeze and before metrics, avoiding reference tables during fitting. Accepted fold provenance binds its row order to the exact scoring artifact before training.

Baseline tests: 4 passed. Affected focused tests: 8 passed in 4.053 seconds, including feature construction, architecture/tie order, train-only normalization/target access, and OOF freeze before reference loading. Compilation and actual-input precheck passed. See T018C_tests.txt and T018C_precheck.json. No formal training or evaluation has run at this entry.

Next: commit source, run ten CPU heads once, freeze OOF predictions, evaluate separately, independently verify saved predictions and arithmetic, report to the research mailbox, and mirror recovery artifacts to both server roots. Stop after T018-C regardless of outcome.

## 2026-09-13T03:08:52.013994+00:00 — formal result verified

Frozen source 8fc63deeb825c87257c46546bc34e2ab3b4bffd8. One formal train and one separate evaluation, exit 0. Ten CPU heads / 120 OOF predictions frozen before reference opening. Literal 5/5; pooled/H0 0.9463400050770442, pooled/oracle 1.0184123344198242, offset/H0 0.874913447259421, left-right/H0 0.9837094741065749, quadrants/H0 1.0. Independent verifier passes; first metadata-dictionary comparison failure and minimal correction are documented in analysis and preserved logs. No experimental rerun or changed science code. Next: evidence PR, main outbox, both recovery mirrors.
