# T060-A preregistration
Exact80 Uouter state0 anchors in persisted order; global y0/grid verified against accepted metadata. No condition-based selection. AST-extract only T051 ev and firstforward z=(base*exp2(ev)).clamp(0,1), not tone/mask/oracle trajectory. Exposure applies RGB-shared to complete current degraded base per explicit task; gate constants remain unchanged feature inputs. Fresh online J original8 differentiable featurechannels;20constant rows. Frozen Ehead/no optimizer. All80fieldtensors fsync/hash before separate source-reference gradients. Bothnorms>1e-12,count>=72,positive>=.80,median cosine>=.40 fixed. No finite-step or quality-metric search. Independent analytic MSE/exposure/bilinear-adjoint gradients, reference-gradient comparison relative<=3e-5 and maxabs<=1e-9+3e-5*maxabs(reference), scalar summaries. One pre-implementation SSH closure recovered before file creation/experiment.
