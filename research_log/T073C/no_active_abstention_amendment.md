# T073-C prospective Ours-TTT no-active abstention amendment

2026-09-25 ~17:00 +08. Authorized by the research-lead correction on `origin/main` commit `068e5fe1` in `coordination/CHATGPT_TO_CODEX.md`. This amends only the formerly undefined input case of frozen T070-A; it does not rewrite T070-A source or change any active-case computation. The amendment precedes the restarted UHD-LL Ours-TTT full batch and uses target-low structure only. References and metrics remain sealed/zero.

For every UHD-LL, LSRW, and Target-3 low input, evaluate the frozen T070-A gate. If and only if its active-region mask is all false, emit the exact frozen Ours-Step0 output/state with zero optimizer updates and record `decision_status=TTT_ABSTAIN_NO_ACTIVE_GATE`, `selected_step=0`. The frozen `research_log/T073C/uhdll_step0_full/output_manifest.json` already records this exact gate evaluation and its low-image SHA256 for all 150 UHD-LL inputs, and its outputs have been independently verified. A UHD-LL batch implementation may reuse an inactive image's frozen Step0 tensor **only when name, low SHA256, frozen execution manifest SHA256, output file SHA256 and output tensor SHA256 match** the Step0 receipt. This reuses an existing scientific result; no target quality or reference is involved.

For a gate mask with at least one true region, call unmodified frozen T070-A `FinalOurs.__call__` and its unchanged trajectory/selector. No threshold, loss, optimizer, selector, lambda, rho or renderer setting changes. The full batch must retain all 150 canonical names, output one decision and full-native lossless tensor per image, and independently verify the two branches and their counts. The known frozen low-only mask count is 49 inactive and 101 active; this is a planned verifier expectation, not a quality outcome. No incomplete prior batch output is silently promoted to the accepted row.

Required focused gate before launch: a test that inactive-mask dispatch does not call the TTT model and records exact Step0 hash/status/zero-update semantics, plus an active-mask test that calls the original TTT path without fallback. A batch-equivalent first active image must reproduce the prior frozen TTT smoke hash. Full-row `FROZEN_OUTPUTS` requires 150/150 manifest, lossless outputs, low-hash check, branch/status counts, output file/tensor hashes, state/provenance checks, and independent verification. No target reference read or metric before the all-Tier-1 output gate.

**Implementation gate, 2026-09-25 ~17:10 +08:** Task-owned dispatcher `run_ours_ttt_abstain_batch.py` SHA256 `0073f16ddf95ac5bef99eaa3be238eb5092d1135b6630d4f86c960b9815a5c1e` reads only the frozen low receipt and independently verified Step0 manifest/tensors. It dispatches active masks to unchanged `run_ours_ttt.run_one` and inactive masks to a bitwise copy of the exact Step0 output, with SHA checks before copy and recorded zero-state/zero-update decision. Independent verifier `verify_ours_ttt_abstain.py` SHA256 `e6019904bfe803c77de45493743cd2501dee51b9a186f6e3853f1822cdb92303` checks all 150 outputs, both branches, 49/101 expected counts, first active frozen smoke hash, complete-case geometry/finiteness and all file/tensor hashes. Focused synthetic CPU tests in `test_abstention_dispatch.py` SHA256 `09301172a5f3a14729584501f42f2d00ee8376d225770b031b991b9d71b3db39` pass **2/2** on the paid host, verifying inactive never calls TTT and active calls the original path; both test outputs pass the independent per-row verifier. This is source-side dispatch proof; no restarted target batch or new target reference/metric has run yet. The batch-equivalent active target-low smoke waits until the static PromptIR GPU run completes.
