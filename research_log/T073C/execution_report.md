# T073-C — Ours-Step0 and Ours-TTT new-card preparation

Status: **frozen method/asset binding and synthetic execution tested; UHD-LL target smoke/full outputs pending**.

The unchanged T070-A recovery archive (SHA256 `3f8d3ece6b0eb96efeb41f89b5d34780f3d1e16080b47492db0595190f1e6237`) was staged on the paid RTX 4090. The original frozen manifest is unchanged, SHA256 `e7f129d931d27531e5f6a14cd72e8c94734c474c40c3c403959cc8f5764e5ab9`. Its 30 source binding hashes match the staged LF-normalized source. The four assets match their frozen SHA256: CLIP `1bd3c7172de5b207ceac554f5ab5266166f3b9baccc9af5989bc801016d080ad`, prototypes `b4b32dbd96c65dcf606ee38d7450ebf348f5731823503b9c71ba15ec78217ac7`, gate `b7458c51466fd89c1bfa8e7c4bf1dd820ddf56e33c12c406f6625ac64361a4ce`, probability model `33f878dd090d5e85ba2d099c0e16bc7e4f1731b41f0c5fd90f2a8a5a30f62be0`. The Windows checkout's CRLF gate copy had a different raw SHA; LF-normalizing it on the execution host recovered the exact frozen asset hash.

`execution_manifest.json` SHA256 `e435c8b8d3b4d814943f9324e48d6e149b75dcb6ad1983b9dd174fc0c72144a2` changes only asset paths and recorded environment from the original; source, constants and asset bytes are unchanged. The new-card environment is Python 3.12.3, torch 2.3.0+cu121, torchvision 0.18.0+cu121, numpy 1.26.4, Pillow 10.3.0, open_clip_torch 2.26.1, cuDNN 8902, RTX 4090. It is **not** the frozen A6000 torch 2.4.0 environment and is not claimed to be cross-hardware bitwise reproduction on historical images. The original T070-A relevant suite passed 7/7 on this host.

Step0 must execute the original zero-raw `CommonRegion2` renderer and frozen gate; simply copying low pixels is not exact in general. For all 256 8-bit channel levels on a miniature input, 192/768 values differed from the zero-raw render by at most `5.960464477539063e-08`. Task-owned `run_step0.py` calls the original `FixedObjective`/`CommonRegion2` with zero optimizer updates. On a 352×352 synthetic low image, its output was **bitwise identical** to the first image of the original 27-update `trajectory`; gzip save/load was bitwise identical. A 3840×2160 synthetic smoke produced finite float32 `[1,3,2160,3840]`, 4,444,867 compressed bytes, whole-run 3.440 s, peak reserved GPU memory 1,337,982,976 bytes. That 4K synthetic gate had zero active regions; active-region equivalence was verified on the 352×352 image.

The unmodified frozen `FinalOurs` inference ran all 27 updates on the 352×352 synthetic image. Its direct `torch.save` persisted a view backed by the entire 28-state trace: a 41,632,855-byte file for one 352×352 output. Task-owned `run_ours_ttt.py` leaves inference/selection unchanged, clones only the selected tensor, then saves it via lossless gzip. Its output is 35,121 bytes. Independent comparison against the original execution receipt passed for decision, selected-state SHA256 `5ca1dc5c8dabf2c5d88e68932d0181756de3bb08c31c7e5e178925675c48278f`, selected-output tensor SHA256 `dda2813a9298257a9a9e47d7724324b08f3d57d984a7acad852adf94b597ebe4`, and gzip roundtrip. This is an output-serialization repair, not a scientific-method change. New-card data disk has 14 GB free; lossless compact packaging is required before any 150-image run.

No UHD-LL Ours model run, reference read or target metric occurred in this stage. Next: one low-only 4K target Step0 smoke, verify output/geometry/hash and resources; then complete 150 Step0 outputs and independently freeze. Ours-TTT needs its own 4K smoke before full execution. Reference payloads remain sealed.
