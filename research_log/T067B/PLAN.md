# T067-B development-only nine-grid calibration

Freeze before reading/hashing any development quality evidence. Use the final frozen T066-A all-development model probabilities, reproduced against the previously frozen T066-B development target-free table; no model refit or LOIO model substitution. This retains the inference model that can later be applied unchanged to transfer. Use exact T063C clipped float64 normalized progress, denominator floor1e-12 and frozenbase. Nine lambda values only. Require endpoint equivalence; no invalid-interval fallback.

Bind each candidate to existing immutable DEV trace/state hash and frozen rendered output hash/images-file hash; existing outputs may be reused. Primary after-freeze scoring uses accepted T062B quality arrays. Independent GPU rerender verifies all unique selected states; independent CPU normal-reference PSNR/SSIM recomputes all28states perimage and checks primary anchors/metrics. Choose all-five-passing candidate by worstT026, then meanT036, then smallerlambda, exactties. Exact frozen metric values used for ranking after independently verifying metrics to1e-10, avoiding roundoff-created tie changes. No transfer artifact/reference access, newcohort, Adam or model fit. Report model-in-sample development calibration limitations, not qualification.
