# T018-D execution log

## 2026-09-13T04:20:02.470781+00:00 — scope and baseline

Research main de3bfaedf95cc2abc2116a362c8c5121f52df7be accepted T018-C (PR27 merge b45bf9563e7c3f7e8aa27c923a35f1aabb95a5cb) and issued T018-D. Branch codex/T018D-final-direction-selector. Read T018D_spec.md. Finalization only: exactly two all-development CPU heads, unchanged recipe, no OOF rerun, fresh cohort, metrics gate, image reads or GPU experiment.

Baseline: 8 relevant direction/energy tests passed in 4.673 seconds. Reuse DirectionHead, fit_head, predict and RECIPE verbatim from accepted ttie/direction_probe.py; reuse saved scoring provenance and accepted target binding. Same local project/code ownership; no external code copied. Python 3.12 / Torch CPU environment unchanged.

Increment 1: add five-feature inference adapter and pinned-receipt loader, testing exact donor equivalence, tie order, serialization, and reference-artifact mutation/removal isolation. Increment 2: add final training/freeze/replay runner, test two train calls and receipt bindings without another formal fit. Freeze source then execute the two final heads exactly once. Independent verifier must replay without decoding target/reference artifacts. It may hash raw training-target bytes to verify provenance, but target values cannot enter replay. No resubstitution metrics are needed.

Reuse map: model/optimizer/normalization/training loop remain in direction_probe; score/target blobs use existing energy_local helpers; only the five-feature API, all-row orchestration and immutable artifact receipt are new. New inference API accepts only center/x_lower/x_upper/y_lower/y_upper, each 28-D (or a batch of 28-D vectors), and emits one decision record per row. No metadata arguments. Loader requires the expected receipt SHA256, verifies model/inference-code hashes, and does not open training targets or reference tables.

Next: implement and pass the bounded inference increment, then finalization increment, one formal fit, independent saved-artifact verification and GitHub/server delivery. Stop after T018-D.

## 2026-09-13T04:23:16.312927+00:00 — implementation green before formal fit

Inference increment 3 tests passed. All affected 12 tests pass in 3.176 seconds; compilation and actual input precheck passed. The orchestration test mocks fit_head to confirm exactly two 120-row calls without extra formal training. It initially exposed a CPU thread-count discrepancy between directly constructed and loaded selectors (exact float replay failed). Moved the existing single-thread setting to the shared selector constructor; subsequent exact replay passes. First failed test log retained. This does not change architecture, features, loss or training recipe. No formal training has run yet. Next: source freeze and one formal two-head fit.
