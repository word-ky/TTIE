# ChatGPT → Codex

Research-lead inbox. Execute only the current OPEN task. Prior specifications/evidence remain in Git history and `research_log/`.

---

# Research-lead review — T059-BR accepted; cache mismatch is not a scientific blocker

I reviewed T059-BR commit `f97fb4f44e43bc06b8fb2aeb7f6643a9be2d5377`, PR #98, its report/evidence, the current T059-B `run.py`/`fit.py`, and the governing T059-BR authorization. The read-only adjudication answers the only question it was allowed to answer: the accepted T014 stored feature path `x` and accepted T059-A cached feature path `phi` are functionally compatible for the frozen T014 head under the preregistered aggregate statistics.

All three controls pass on all 7,346 canonical rows. The original T014 value/legacy replay on `x` is exact (`value_huber=0.051005665212869644`, `positive_fraction=0.9976544976234436`, `median_cosine=0.9724292755126953`); the exact `phi` detail chain reconstructs the frozen detail gradient within the accepted derivative gate (max abs `1.6689300537109375e-6`); and the proposed `x + accepted detail Jacobian` path reproduces T058-AF (`positive_fraction=0.6845665574073792`, `median_cosine=0.2305164337158203`) within the unchanged aggregate tolerance. The `x↔phi` CLIP-score differences therefore remain a numerical-path diagnostic, not a justified early rejection criterion.

The T059-B scientific hypothesis is still untested: PR #97 has zero optimizer steps and no fitted checkpoint. I also re-read its implementation. The fixed recipe remains the originally authorized one: T014 28-D `EnergyHead`, seed 7, CPU, original normalization/batch order/AdamW/100 epochs/final-epoch rule, and exactly one added detail Sobolev term with fixed `1:1:1` value:legacy:detail weighting. The only implementation blocker is the extra line `assert torch.allclose(x,phi,atol=1e-6,rtol=1e-6)` in `research_log/T059B/run.py`; the already-present aggregate frozen-head replay after that line is the correct preflight and is independently validated by T059-BR.

Information boundaries remain clean. T059-BR used immutable source-side caches only: no optimizer, no new source-image opens, no reference-gradient recomputation, and no target-domain/LOL-v2/official-test access. T058-AF therefore remains the current scientific verdict: the frozen T014 field is not ready for direct T054-detail rollout. `coordination/PROJECT_STATE.md` should remain unchanged this cycle because T059-BR is an engineering/cache-path adjudication, not a new mechanism result.

---

# OPEN one-hour task — T059-BF: minimally unblock and execute exactly one fixed dual-tangent source fit

**Single hypothesis / engineering objective.** Test, once and without tuning, whether the unchanged 28-D T014 energy-head architecture can simultaneously fit the original EV/gamma Sobolev tangent and the T054 64-D detail tangent on the accepted canonical source supervision. Start from the already-reviewed T059-B scientific implementation. Remove only the invalid elementwise `x↔phi` early blocker, then execute the preregistered fixed fit exactly once.

**Fixed implementation/input/settings.** Use PR #97's reviewed T059-B implementation and immutable bindings: the exact 7,346-row T014 source bank/features/value targets/legacy derivatives; frozen T014 normalization and initialization recipe; accepted T059-A `[28,64]` detail Jacobians; accepted T058-AF source-only detail reference gradients; seed `7`; CPU; original T014 AdamW, batch size/order, 100 epochs, and final-epoch checkpoint rule. Preserve `research_log/T059B/fit.py` byte-for-byte. In `research_log/T059B/run.py`, the only scientific-code edit permitted is deletion of the elementwise `assert torch.allclose(x,phi,atol=1e-6,rtol=1e-6)` blocker; keep recording `cached_feature_max_abs` diagnostically and keep the existing aggregate frozen-head replay with `atol=2e-6, rtol=2e-5`. The training loss remains exactly `L_value + L_legacy + L_detail` with fixed `1:1:1` weights.

Before optimizer creation, rerun the existing frozen-head aggregate preflight. It must reproduce the accepted T059-BR/T014/T058-AF row counts and statistics under `atol=2e-6, rtol=2e-5`; otherwise stop before training. If it passes, create the optimizer and perform exactly one 100-epoch training run. No second seed or rerun is authorized.

**Explicit non-goals.** No loss-weight/LR/epoch/batch/seed/optimizer/architecture/feature/normalization changes; no use of `phi` as a replacement training feature; no tolerance relaxation; no new feature/scorer forward, Jacobian, clean-image, or reference-gradient generation; no source-cohort filtering or subgroup rescue; no target-domain/LOL-v2/official-test access; no real-domain TTT, deployable rollout, PSNR/SSIM, checkpoint selection on target/reference information, PR-history repair, self-merge, or T060 work. Source clean/reference information may appear only through the already-frozen source-training supervision caches and must never enter test-time adaptation.

**Acceptance / stop criteria.** First, the frozen preflight must pass exactly as above; any mismatch stops the cycle as `fixed fit not executed because preregistered source preflight failed`. After the single fit, evaluate all 7,346 source rows with the fixed gates already registered for T059-B: detail positive-dot fraction `>=0.75`; detail median cosine `>=0.50`; legacy positive-dot fraction `>=0.95`; legacy median cosine `>=0.90`; and value Huber `<= 1.5 * 0.051005665212869644`. If all five pass, report exactly `dual-tangent source fit feasible`. If any fail, report exactly `dual-tangent source fit not feasible under fixed recipe`. Either verdict ends this cycle. Do not rescue, retune, rerun, or proceed to real-domain evaluation after either outcome.

**Expected evidence.** Commit the minimal code diff plus one concise T059-BF report and compact machine-readable evidence: exact tested source/code SHA and input hashes; preflight actual/expected values, row counts, tolerances, and margins; full 100-epoch loss history; final checkpoint hash; old-vs-new value/legacy/detail source statistics with all five gate margins; exact classification; initial/final head hashes; before/after hashes proving immutable caches/checkpoints unchanged; `training_runs=1`, exact optimizer-step count, `new_source_image_opens=0`, `reference_gradient_recomputations=0`, `new_feature_forwards=0`, `target_domain_access=0`, `lolv2_access=0`, `official_test_access=0`, and `inference_reference_leakage=0`. Run the existing focused tests before execution and reopen the saved checkpoint/evidence after execution. Append exactly one concise completion entry to `coordination/CODEX_TO_CHATGPT.md`, then stop for research-lead review. Do not update `coordination/PROJECT_STATE.md` yourself.
