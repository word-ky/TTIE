# T039-A source gain-tangent audit

Use all7346 accepted canonical source-training bank rows in manifest order, including fixed duplicate entries; 400 banks across80 source IDs. No outcome-driven selection/subset. Probe gains0.75,1.00,1.25, allactive regions share the specified physical gain, inactive gains stay1; original raw EV/gamma and gate are fixed. Total22038 probes, no optimizer/selector.

Stage A reads only accepted stored source-bank tensors, not JPGs/clean targets/target labels. The retained bank identity tensor supplies the input; canonical gain1 reconstructions must agree with saved bank outputs <=1e-6 and cached features <=1e-5 before probing each state. Pin T014/T031 receipt, manifest, all bank files and energy/checkpoints. Reproduce frozen normalization from all7346 source features. Save inputs, raw states, g_E, features, energies, masks and output SHA256s. Hash every probe output; do not retain redundant full22k output tensors because Stage B reconstructs and verifies them bit-exactly. Freeze complete Stage A before any source JPG reference decode. Clean-condition bank identity is used solely in its already-retained input role; no reference target/label is loaded in Stage A.

Stage B opens only80 accepted train_t014_sobolev JPGs using exact T014 load_image (shorter320, bicubic antialias), computes float64 RGB-MSE g_R at frozen probes, verifies output hashes and no raw changes. Accepted T029 active-coordinate double dot/norm/cosine convention, degenerate norm<=1e-12 excluded from cosine/positive fraction, reported explicitly. Independent standard-library scalar replay for all probes. Source gain-tangent deficit supported iff gain positive fraction is >=20percentagepoints below legacy AND gain median cosine >=.25 below legacy; else source gain-tangent deficit not supported / real-domain effect remains plausible. No secondary gates.

GPU1 both stages. Local focused tests2passed8.26s. Seven direct T014 donor module hashes equal accepted source receipt; source/bank/checkpoint provenance and fixed choice frozen before probe computation. Source bank outputs total12.604GB; reusing all is feasible within the one-hour task. No training, LOL-v2 images, fresh cohort, controller or official test. Stop after the audit/report.

Observed preflight failure: initial run082440 stopped at receipt hash before source-bank tensor access. Accepted T031 receipt SHA d437... is the CRLF representation of the Git LF blob c6c611...; exact LF-to-CRLF bytes reproduce the accepted hash with identical parsed JSON. The bound historical representation is preserved in T039A_bound_T014_receipt.json and passed explicitly, original metadata remains unchanged. Bank/source manifest and checkpoint hashes already matched. No probe/reference work occurred in the failed attempt.
