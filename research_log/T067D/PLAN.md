# T067-D frozen interval diagnosis

Reuse accepted target-free T066A history and T067C choice/model/rule hash bindings. Build all inclusive [k_FS,k_rho] interval rows, progress/q/state/renderer identities; freeze before even hashing reference labels. Primary joins accepted T066B labels after freeze. Independent GPU rerenders all28frozen transferstates perimage and CPU recomputes normal-reference PSNR/T026 margins, covering allintervalstates; no optimizer or model fit.

Boundary bookkeeping: any unsafe includes k_FS itself because the required interval includes it. Report first unsafe within interval plus an additional strictly-after-k_FS field and whether initial state is reference-safe; if initial state is unsafe, contiguous safe prefix is empty/null. Recovery means any subsequent safe state after first unsafe. Selected relation uses first unsafe interval boundary; no boundary is explicit. q is exactly the prescribed formula, without clipping. Since k_rho can overshoot rho, q may exceed1.0001; keep four fixed bins unchanged and explicitly report outside-bin rows/count rather than clipping or dropping them. No cutoff/selector is derived. All labels/boundaries remain offline diagnostic.
