# T068-C frozen endpoint diagnosis

Reuse exact T068B source86b99ad7261bc9ac4b1b4fad089f616137de0d27 and target-free development curve freeze, T067C endpoint freeze and T066A transfer history. Baseline remote12passed1.83s and independent2041renders/2800quality states PASS; no new third-party code. Existing GPU RMS function and independent CPU dot RMS retained. Same model/renderer/no refit/optimizer.

New core only fixed three-transition ratio, empirical nearest-rank T99 sorted[98], strict above flag and diagnosis. First focused window/floor/singleton/percentile tests, then adapter + affected tests, source commit/push, real GPU end-to-end diagnostic and independent verifier.

Development scores reuse already-bound target-free d_j from T068B. Recheck endpoint/model/input binding and exact frozen choice; do not read development quality. Transfer rerenders all interval states on GPU, computes exact float64 RMS, scores fixed T067C endpoints. Freeze development100score table/T99 before transfer, then fulltransfer100 before reading/hashing reference-quality evidence. Include all motion components/state/render/input/rule hashes and reference_reads=0. No selection change.

Primary join accepted T067C per_image.json endpoint PSNR/T026 margin only post-freeze. Independent join accepted T066B transfer_labels.json crosschecks exact same selected endpoint margins (no new reference scoring). Independent verifier rerenders both cohorts, reconstructs model endpoints, recomputes RMS via CPU float64 dot-product, scores/T99/flags/diagnosis. Numerical components tolerance1e-12, exact endpoint flags/diagnosis; no epsilon tie substitute. Rank=1+number of strictly larger scores (ties share rank); empirical percentile=(number of scores<=R)/N, both descriptive only. All unsafe endpoint ranks plus counts/ranks for allsafe endpoints in evidence.

No reference reads for development, no fresh/final set, no guarded-output metric, no threshold/window search, no newselector. Undefined total signals use stated denominator floors, only singleton override. Stop after one diagnosis/report.
