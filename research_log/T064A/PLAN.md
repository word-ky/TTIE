# T064-A frozen fresh-tail reachability

Reuse the accepted T063-A render/oracle/evaluator/verifier workflow on exact accepted T063-D inputs. Baseline3tests pass13.93s;current3tests pass9.26s including selected checkpoint11. No optimizer run or selector change. Before diagnostics, re-render100x28states on A6000GPU1, matching frozen low hashes and all100 accepted selected-output tensor hashes. Standalone low PNGs and traces only during reconstruction; no historical outputs container or reference manifest/metric reads. Reference metadata and original controls/selected tensors are read only after the global2800freeze; exact torch.equal is additionally checked then.

The oracle is unchanged: maximum PSNR within deltaT026>=-5.614, earliest tie; empty safe set reported unreachable and unconstrained max shown only diagnostically. Classify FRESH_TAIL_SELECTION_LIMITED iff100/100reachable and allfivegates pass, otherwise FRESH_TAIL_TRAJECTORY_LIMITED. Label REFERENCE_ORACLE_ONLY throughout. Save all2800metrics,reachable sets,oracle choices,and complete28-step failure trajectories for indices16/86. Independent GPU rerender plus independent PSNR/SSIM and oracle/gates/read-order replay. Stop after report; no deployable rule, tuning, new cohort or held-out access.
