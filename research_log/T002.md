# T002: spatiality, capacity and prior controls

## 2026-09-11T17:55:00Z — Task received and predeclared experiment

T001 was accepted by the research lead and PR #1 merged at 17:49:37Z. Main revision `54362b1f661777d26d56592f4594323ee5f944a5` includes OPEN T002. This heartbeat read the reply and started the new task, confirming a real GitHub-mediated round trip. No research-owned mailbox/state edits.

Branch: `codex/T002-spatiality-controls`. Reuse T001 ISP, fixed local_statistics_loss, fresh Adam episodes, clean-only offline evaluator, Pillow image rendering and remote execution workflow. No new loss or downstream models.

Predeclared BEFORE T002 experiments:

- Seeds: 7, 11, 23 (noise realizations of the same controlled textures, not independent natural scenes).
- Fixed Adam 200 steps, lr 0.03, identical zero initialization/bounds/prior. No per-condition tuning or checkpoint selection.
- Models: identity; global 6 scalars; uniform_control with 16 raw six-vectors (96 scalars) averaged BEFORE bounding, then one physical vector broadcast everywhere; spatial 1x1, 2x2, 4x4, 8x8 (6,24,96,384 scalars).
- Uniform latents all start at zero. Symmetry gives identical latent updates. Their rendered function still has only 6 degrees of freedom; this is a latent-count control, not a globally uniform expressive nonlinear ISP capacity control. Adam epsilon can cause small differences from global because each raw gradient is divided by 16. Report these rather than claiming independent capacity equivalence.
- Three 64x96 clean families: unchanged T001 midtone; same texture with intrinsic dark rectangle/disk structures multiplied by 0.35; high-key `0.75 + 0.5*(midtone-0.5)`. These content transforms are not supplied to adaptation.
- Eight conditions: clean/no degradation; homogeneous x0.45; homogeneous x1.55; left/right x0.45/x1.55; alternating 2x2 quadrants x0.45/x1.55; smooth horizontal gain 0.45->1.55; alternating vertical stripes with 4 cycles (12-pixel bands); alternating stripes with 12 cycles (4-pixel bands). All multiply then clamp [0,1]. Same masks for every family/seed. High-frequency stripes also diagnose limitations of the 8x8 patch-mean loss, so grid resolution and objective aliasing must not be conflated.
- Full matrix 3 seeds x 3 families x 8 conditions x 7 models = 504 rows / 432 adaptation episodes. Run once on A6000 CUDA. CPU tests and focused cross-device/repeat checks validate software; no repeated full sweep needed unless a real failure requires repair.
- Metrics per row: loss before/after, MSE/PSNR (PSNR null for exact-zero MSE in JSON), left/right and quadrant MSE, input-output drift MSE (equals identity drift for clean condition), raw parameter count, six physical field spatial variances, input/output clipping fractions at 0 and 1, NaN/Inf. Retain outputs/grids and full diagnostics for seed-7 illustrative cases. Summaries report complete seed means, not picked winners.
- Fixed figures: all midtone seed-7 conditions with clean/input/global/uniform/spatial4/spatial8 outputs and EV fields; all three seed-7 undegraded content controls. EV uses fixed [-2,2] color scale; full six-coordinate field variance and grids remain machine-readable.

Implementation increments: (1) uniform control + tests, preserve T001; (2) suite generation/evaluation/exports and leakage/condition tests; (3) full regression, fixed matrix on A6000, inspect artifacts, report hypotheses honestly. Appropriate evidence, no extra framework or speculative validation.

## 2026-09-11T18:01:00Z — Increment tests

- Fresh T001 baseline at main: `python -m unittest discover -s tests -v`: 13 passed in 5.698s.
- Uniform-control increment: `python -m unittest discover -s tests -p test_uniform.py -v`: 3 passed in 3.139s. Arbitrary latents aggregate to uniform field, all 96 raw gradients finite/nonzero, symmetric raw states remain equal, global/control close within 1e-5 for the test, reset reproducible, spatial1/global bitwise equal.
- Suite increment: `python -m unittest discover -s tests -p test_suite.py -v`: 4 passed in 2.986s. All content/condition constructions, T001 degraded-image equality, stripe transition counts, input-only adaptation boundary, evaluation-reference replacement, region/variance/drift metrics and CSV/JSON/PNG export smoke passed. No hyperparameter changes.
