# T073B PromptIR static 150-image batch launch plan

2026-09-25 16:50 +08. The independent one-image native static execution receipt is `target_static_schedule_smoke.json`. The full-batch runner `run_static_native_batch.py` SHA256 `c1c8dc881970880f699905aaf74a66caa6ca46eedb14ba846167a01044adea3a` was exercised with `--smoke-one` on the same canonical low input. Its lossless stored float32 tensor SHA256 is `edd0d508e737f5460cb93e2cc153479b3f72e9948ab9ebb19d75ca1c1d00db2b`, **identical** to the independent smoke. Its one-row manifest SHA256 is `bb6957da9329cc3f45ba39e2721e586370a9bf9a88af25b40a8984286b1fef2d`. Checkpoint, low receipt, and 548-key model state matched.

The full run will read only the frozen 150-name/low-SHA256 receipt and each corresponding low image. It uses the same official source/checkpoint, float32 native geometry, model seed and three-part sealed memory schedule. It saves each output as lossless `output.pt.gz` plus a per-image decision receipt, then a 150-row manifest. No references/metrics are accessed.

Capacity observation: the one-image lossless output is 86 MiB; 150 could exceed the data disk's 11 GiB free. The remote root overlay has 29 GiB free. Therefore the full output root is **`/root/TTIE_T073B_static_full`**, not the 11-GiB data disk. This is instance-local and must be fetched/verified into the project before instance teardown. Batch launcher/log/exit receipt are under `/root/autodl-tmp/TTIE/T073B/runs/` and their paths will be recorded at launch. No row is frozen until independent verification of all 150 lossless tensors, source low hashes, geometry, finiteness, and manifest.
