# T073-B independent execution-path review — 2026-09-25

Decision: **B3a low-only binding accepted; B3b native-4K smoke pending.** This review uses the pinned source files and completed tests/remote receipts, not a target quality metric.

- Both rows use the identical `epoch=80.ckpt`, SHA256 `206baf0dd10f636f025b33b5ee7eb63a353fcbf4d50858b62f9480a6d4be9d4a`; strict load matched all 548 keys. The remote official checkout is commit `0526bf7b87c2a54574ae1cb3af916fd5568fc0b1`. Local and remote `tta.py` both match Git blob `53d3071b859b43aa2b42c24c3c98608eacf11b09`. Their raw SHA256 differ (`d101d7f5...` on Windows, `7d10adf8...` on Linux) solely because of checkout line endings; `git diff -- tta.py` is empty on both hosts. The only official-source edit is the recorded lazy import in an unused DCN class.
- The official paired entrypoint opens target GT and is excluded. The task runner has no GT/reference argument and constructs only the task-owned low-directory training/inference datasets. `tta.SRTTA::compute_fisher` moves its third tuple element to CUDA but never consumes it in loss or Fisher selection; the adapter supplies the degraded tensor in that slot. The adaptation call receives degraded tensor, basename and RDDM; no clean tensor. The RDDM train call uses degraded input and teacher pseudo-target. No target reference path is staged under the task run directory.
- Synthetic comparisons covered exact degraded patch order and inference tensor matching; the unchanged upstream SRTTA Fisher/update/wavelet/optimizer produced exact paired-vs-low-only toy-state equivalence. The toy restorer/redegradation/loss limit this proof; full PromptIR+RDDM+VGG was additionally exercised end-to-end on low-only synthetic input, not against a paired full-stack oracle. GT-open mutation raised `PermissionError`. Four focused tests passed.
- The pre-existing remote low-image cache was checked against the frozen T072-I per-file SHA256 list: **150 expected, 150 present, zero mismatches**. This is a low-only payload check; no reference was opened. B3b must still run one native-4K static and DCTTA execution-only smoke, check geometry/dtype/finiteness/runtime/VRAM/hash, then complete and freeze all 150 outputs. An OOM is not permission to change scientific computation or inspect references.

Target model runs before this decision: 0. Target reference reads: 0. Target metrics: 0.
