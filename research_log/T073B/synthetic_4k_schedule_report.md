# PromptIR native-4K execution schedule probe — synthetic only

2026-09-25 15:48 +08. Status: **not accepted; no target retry**.

The official 4K PromptIR smoke had failed at `Attention.qkv_dwconv` with PyTorch's 32-bit convolution-index assertion. A task-owned row schedule (`chunk_conv_schedule.py`) was tested without touching the official model weights or architecture: each depthwise 3×3 convolution uses the same original module on height chunks with a one-row halo, concatenating only each chunk's interior. On a 144-channel synthetic 320×320 GPU input, original versus scheduled depthwise output was bitwise identical (maximum absolute difference 0). The full checkpoint-loaded PromptIR 352×352 synthetic output was also bitwise identical under the forced schedule for all attention and FFN depthwise modules, or the probe would have asserted before native execution.

The first native-size 2160×3840 **synthetic-only** full-model probe with attention depthwise scheduling progressed beyond the previous 32-bit indexing point but failed at `FeedForward.dwconv` with an observed 15.76 GiB allocation OOM. A second probe also scheduled FFN depthwise convolution; it progressed further but failed while concatenating its 4K output at `decoder_level2.noise_level1`, with an observed 6.57 GiB allocation OOM on the 48 GB RTX 4090. The full 4K PromptIR output is **not** available, so this schedule is not a validated execution-equivalent rescue and no UHD-LL target retry is authorized. It is not legitimate to resize or whole-image tile because the latter would change global attention normalization.

Code SHA256: `chunk_conv_schedule.py` `817e0cc7ae71fb9d165c9e47537fbc076b5cb179b445c05fed640f1989478733`; `probe_chunk_conv.py` `ab5f3b443101b12903cc5cadc44cf5b61745dbca50e91d2c3bf77e04277ebf16`. The probe reads only a synthetic tensor and the already-pinned official five-task checkpoint SHA256 `206baf0dd10f636f025b33b5ee7eb63a353fcbf4d50858b62f9480a6d4be9d4a`; `reference_reads=0`, `target_model_runs=0`, `metrics=0` for this probe. B3b remains `BLOCKED_EXECUTION_4K_INDEX_OR_MEMORY`, with zero accepted PromptIR/DCTTA native outputs. Further operation-level memory scheduling would need separate prospective synthetic proof; do not treat this failed test as a protocol change.
