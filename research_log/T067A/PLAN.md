# T067-A frozen first-safe audit

Reuse exact T066-C events and T066-B target-free probability/render table. Reconstruct earliest probability>=.5 within frozenbase; require equality to non-null first_safe. No fallback, offsets or persistence. Re-render only selected frozen12Dstate on GPU1, bind low hash, trace hash, state tensor hash and output hash. Reuse accepted target-free read scope: only low/trace reads until output freeze. All100selected outputs frozen before reading/evenhashing evaluation metadata. Exact accepted T063B five gates; compute all-five verdict directly (its calibration-only eligible field is omitted). No Adam or model fits.

Reuse accepted T066A score_transfer for bound exactT026/T036 and reference loading; primary NumPy mean MSE/RGB-SSIM; independent CPU dot-product MSE/separable SSIM. Independent code reconstructs earliest step separately, GPU re-renders all100selected outputs, verifies hashes/readordering, recomputes allmetrics/gates/histogram/tails. Binding/package includes evaluation metadata separately after freeze to avoid prior packaging omission. This exposed-cohort result is method-development only; full-trajectory-derived features preclude a compute-saving online-stopping claim.
