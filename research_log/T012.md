# T012 — source-trained label-free trajectory quality head

## 2026-09-12T06:52:00Z — accepted task and frozen implementation choices

T011 accepted/PR11 merged4c02ad3d. New T012 OPEN on main3ef61eeb446da08eda8133a23bbbffbeb59b3b66. Branch codex/T012-learned-trajectory-stop starts there. Reuse the accepted T011 generator unchanged; learn only checkpoint quality/selection. Baseline command D:/anaconda3/python.exe -m unittest discover -s tests -v; receipt T012_baseline_tests.txt.

Reuse map: projected_ttt.run_candidate/FixedObjective/Region2/Adam own the exact trajectory; capture scorer inputs/scores with a passive recorder, without any extra model forward or gradient change. Checkpoints are the actual images passed through the frozen scorer, including zero-state step0; no-active episodes use exact original identity. Keep all-inactive selection bypass. Derive 33 features solely from captured gate/scores/grids/losses/gradients/projections. stop_quality owns only new schema/MLP/train normalization/argmin. Reuse restoration_metrics for reference-only checkpoint metrics, residual_data metadata selection, fixed metrics/serialization/AutoDL conventions. No donor from another repository or dependency/license changes.

Feature conventions fixed before source data/outcomes: 33 float32 entries in the exact task order. Row-major quadrant order. Signed winner +1 dark/-1 bright/0 inactive; original normalized winning evidence is the unchanged T007 winning_evidence (not clipped and retained for inactive views). Current z=(score-tau)/scale, unclipped, all four views. EV/gamma physical values use persisted T011 post-projection grids. Step index i/40; loss delta L_i-L_(i-1),0at0; outgoing raw-gradient norm,0at terminal; cumulative exact pre/post raw-change hit count divided by i,0at0. No metadata/labels/references in feature or selector APIs.

Fixed-step candidates0,1,2,4,8,16,40 use checkpoint min(k,last saved step) when the original semantic stop terminates early. No extra updates to reach a candidate depth. Source fixed-step calibration chooses eligible cleanp95<=.005 then minimum pooled LR+quadrants MSE, exact ties smaller step. Oracle is earliest minimum reference MSE and evaluation-only. Learned scores are predicted log(MSE+1e-6); exact score ties earliest. No selector may create a new correction.

Head recipe:33->64->64->1,ReLU,Huber defaultdelta1 on standardized targets; AdamW lr1e-3,weightdecay1e-4,defaultbetas/epsilon;batch256,100epochs,seed7,shuffle everyepoch,lastpartialbatch retained;finalepoch only. Fit population mean/std on train checkpoint rows only; constant coordinates use scale1. Train on CPU with one thread and independent seed7 shuffle generator. Canonical head inference is CPU for the same persisted features; test repeatability on CPU and CUDA plus cross-device decision parity on fixtures. The frozen CUDA trajectory retains its previously disclosed backward nondeterminism; no bitwise trajectory-repeat claim on A6000.

Data barrier: deterministic100development images excluding308previously used; first80train,next20calibration,fiveconditions. No fresh evaluation manifest/image loaded before Stage A passes and receipt/head/normalization/schema/manifests/source/assets are committed. If Stage A fails, stop. If it passes, verify the committed receipt bytes against git before fresh manifest creation, then verify its head/assets/schema/code/manifest identities before Stage B scoring. Stage B manifest also committed before outcomes. Fixed methods and11qualificationclauses exactly inbox;offsetstress report-only. No future modules.

Increments: (1) passive trajectory capture/features/selection with T011 parity and reference-independent tests; (2) fixed train-only MLP recipe, deterministic scores and source/heldout metrics/step-selection rules with tiny tests; (3) staged data/persistence/receipt barrier/runner, mocked reference replacement and save-before-reference integration, then full local+remote suites and real Stage A. Persist checkpoint image packs remotely; small states/features/scores/targets/checksums and all reports locally. No tuning from calibration outcomes.

## 2026-09-12T07:12:20Z — implementation gates green, source preparation next
Baseline93 tests passed26.953s. Increment trajectory/features4 pass7.970s, fixed quality2 pass7.315s, metrics2 pass.984s, staged source/freeze/persistence4 pass15.956s. Final full105 tests pass31.555s. No failed tests in this task so far. A tiny synthetic source/calibration fixture exercised all100 training epochs, source fixed-step selection and the negative-source stop; its outcome is a software fixture, not research evidence. It created no fresh evaluation manifest. Core T011 semantic/projected files remain unchanged; only the existing metadata selector gains count/split arguments with identical defaults.

Source training uses every train checkpoint equally; normalization population statistics are computed in float64 and stored asfloat32. Canonical predictions are unstandardized log-MSE scores. Method metric steps records selected checkpoint index; full generation updates remains explicit in diagnostics/trajectory. The head still runs the full original trajectory, so this is selection, not a compute-saving early-exit claim. Oracle output is an index into the already-hashed checkpoint image pack, never a new correction.

Stage-B source/fresh procedure: if A passes, copy head and receipt to project research_log, commit/push, invoke verify_git_receipt on the actual committed receipt blob locally, save its commit/hash verification under project logs, and only then supply it to metadata-only fresh preparation. The Stage-B runner independently checks head/schema/normalization/source-code/source-manifest/model/gate identities and the committed receipt hash recorded in the fresh manifest before loading new images. Both stages are implemented/tested, but StageB cannot run after a failed source gate.

Remote has201GBfree. Observed unrelated tmux jobtaisp-t009-coco1000 is preserved. Device0 free42.05GiB,device1 free47.12GiB; launch T012 with CUDA_VISIBLE_DEVICES=1 (logical cuda:0 = physical second A6000). No scientific parameter change or interference with the other job. Use a preparation-only release to create the source manifest, commit it, then deploy final frozen source before starting outcomes. Full checkpoint image packs remain remote; all features/scores/rawstates/decisions/reference-only targets/reports will be fetched.

## 2026-09-12T07:14:12Z — source manifest frozen before outcomes
Preparation-only release20260912-151251-ttie-t012-prepare generated100source images, excluding308prior usedIDs. Train80 IDs12748..20992; calibration20 IDs21167..23359, exactfullnamesinT012_source_manifest.json. ManifestSHA256bcd0f7ca780182dab34e6bc2f83cd50e65a58458c689edbc49eb2b5b837ff6f7. Fixed representative calibrationID21167. Onlyimageheaders/hashbytes inspected; no source adaptation/targets/training performed yet. No fresh evaluation manifest exists. Final source+manifest commit/push precedes real500input StageA.
