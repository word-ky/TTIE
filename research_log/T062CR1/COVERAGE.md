# T062-C-R1 provenance coverage and reuse

Authorization ffe89df73bce4d30773b9401af3d92974a453cb3. This is a metadata-only cohort repair; no inference or efficacy evaluation.

Base source is the accepted T036 exclusions ledger: 689 Train pairs,316 previously reference-used. T036 manifest adds100. Its accepted provenance dependencies are rebound and verified, including the documented historical CRLF digest for T023 source receipts. Only normal paths and the boolean nonempty excluded_by enter exclusions; no condition/outcome/metric value enters selection.

Discovery pins77 preauthorization remote engineering branch tips for tasks T036 through T062, including branches absent from the working ancestry. It examines1526 task-owned JSON files for explicit opens/reads/opened fields and dedicated opens/reads files. The committed discovery records contain paths, access flags, and immutable source identities only.176 access-bearing records are included in the explicit allow-list. Scope includes earlier blocked attempts as well as completed tasks; all actual observed reference access matters for never-open freshness. No later branch or current task is included.

The discovered post-T036 Train normal-path union contains200 paths: the T036 development100 and earlier T022 validation100. Both are already excluded by the base union. Examples include T053/T056 receipts obtained from their own engineering branches, T059-X/Y normal read logs, and T060-C-R1 normal read log.

Cases without dedicated path-open JSON were reviewed and bound:
- T055-V replay opens the T022 split; its run.sh names research_log/T022A_data/split.json. Those100 already belong to the historical ledger. Replay source, run command and split are bound.
- T058-AB/AC prohibit image opening and work on existing source banks; both runner sources are bound.
- T059-BR works on existing source features/derivatives; T059-C is a manifest-only audit. Bound runner sources identify their scope.
- T061-C performs frozen table lookups without image decoding. Bound analysis source identifies this scope.
- T062-A evaluates only the hash-bound T036100, as shown by infer/evaluate source and reference-open marker. T062-B uses the same hash-bound T036 manifest, as shown by analyze.py and inputs.json. They cannot add paths outside the existing union.
- T039/T040 and later source-direction work list COCO source JPGs or existing banks, not additional LOL Train normals. These access records are bound but add no Train pair.

Thus all identifiable accepted prior Train-reference accesses have coverage. There is no unresolved missing/mismatched source or unnamed additional Train reference access in this audit. The union is416, leaving273. This is project-reference-use freshness under the accepted ledger convention; historical encoded file hashing/IHDR inventory is distinct from opening reference pixels, as already recorded by T036.

Reuse: accepted inventory, exclusion reasons as booleans, and exact immutable access receipts. New code only binds path projections, selects the specified SHA256 prefix cohort, and independently verifies union/order/hash. Tests cover exact ordering/exclusion, insufficient remaining pairs, and nonconsumption of extra quality/condition fields. No tests or code alter the method, objective, step27, or acceptance thresholds. Independent verifier does not import the selector and uses binary digest heap selection from original bound sources.

On COHORT_FREEZE_PASS, stop and await lead authorization for any model experiment.
