# T004 frozen CLIP signal audit

## 2026-09-11T20:43:00Z — Task receipt and predeclaration

T003 accepted at https://github.com/word-ky/TTIE/pull/3#issuecomment-5640272692 and merged as 862e3401cf28e9d08582996dad2eb974ad5f9617. Heartbeat received OPEN T004 on main bdec73d. Fresh branch codex/T004-clip-signal. Baseline 27 tests passed in 11.419s (T004_baseline_tests.txt). Read protocol, state, complete task, prior handoff and remote records.

Use existing ISP/adapt/test/deployment code. Stage A requires new natural-image loading, frozen OpenCLIP scoring/calibration, and an offline signal audit; no alternate trainer. Implement Stage B only if the Stage-A conjunction passes. Preserve all previous regression tests. Do not add teacher, detector, learned prompts, T005 or further regularizer sweeps.

Before any CLIP score is computed: model ViT-B-32 / laion2b_s34b_b79k via open_clip_torch 2.26.1 (MIT upstream, public API, no copied implementation); all model parameters frozen, eval, float32. Existing remote torch 2.4.0+cu121 remains unchanged; install compatible torchvision 0.19.0 and OpenCLIP only in TTIE venv. Official sources: https://github.com/mlfoundations/open_clip and https://cocodataset.org/#download . Checkpoint SHA256 and full package versions will be recorded before evaluation.

Image source: existing COCO2017-validation image-only cache /home/liujianhua/wjq/TAISP/shared/coco200/val2017 (200 JPEGs). No annotations, labels or prior experiment metrics read. Selection rule: sort these available COCO image filenames ascending, retain minimum original side >=320, take first18; first6 calibration, remaining12 held out. This uses an existing 200-image pool, not the first18 of all5000 validation images. Pool provenance beyond COCO image IDs is not established, and the pilot does not claim random/representative sampling. Copy only selected images into TTIE/shared; commit IDs, hashes, source URLs, dimensions, exclusion/selection rule and preprocessing, never image files. This remains the requested COCO source, not substitute data. Selection is entirely file metadata, fixed before any image scores.

Input geometry: load RGB float32 [0,1], resize whole image to shorter side320 preserving aspect ratio (bicubic antialias, clamp to[0,1]); no square distortion. Six sRGB conditions: clean, homogeneous .45/1.55, left .45/right1.55, alternating quadrants starting dark top-left, horizontal linear .45..1.55 gradient. Same existing gain conventions generalized to arbitrary height/width. Five fixed views: full/TL/TR/BL/BR using integer half splits. CLIP views resize shorter side224 and center-crop224, bicubic antialias, frozen model mean/std. Use identical differentiable geometry for scoring and any later adaptation. No mask-dependent cropping.

Prompts are exactly the nine strings in T004 inbox, embedded individually, normalized, averaged within each concept and renormalized. Save them in code/config before evaluation. Calibration uses only six clean calibration images x5views: each score threshold linear-interpolated 95th percentile; std scale uses population std (correction0), floor .01. Winner argmax(d_dark,d_bright), ties choose dark; active only when winner > its corresponding threshold. Calibration constants persisted before held-out scoring; no percentile/model/crop/prompt tuning.

Stage A: all12 held-out images x6conditions x5views=360 rows, plus30 calibration rows. Report clean false activation; any activation TPR and correct-type TPR for each homogeneous degradation; correct type among active homogeneous-degraded views; pairwise ROC AUC with ties counted .5 for dark-vs-clean and bright-vs-clean; corresponding-score strictly positive paired changes, ties failures. Report aggregate plus full-view/quadrant breakdown; heterogeneous scores retained without inventing a single ground-truth type for mixed views. Gate uses all60 clean views and each homogeneous set of60: FPR<=.15, each AUC>=.75, each paired increase fraction>=.75. No held-out adaptation before this gate. Failure stops task with raw scores retained.

If Stage A passes, freeze Adam40/lr.01 for every adaptive baseline, EV/Gamma-only explicit mask, existing physical bounds, no old prior in CLIP loss, mean softplus over original-input active views. No-active objective is exact output-connected zero; inactive episode must return identity. Compare identity/old global/old spatial4/CLIP global/CLIP spatial4 on same72 inputs; no condition metadata or clean target enters adaptation. Further Stage-B implementation/tests and fixed panel selection will precede any held-out adaptation. No evaluation outcomes have been inspected at this declaration.

Increment sequence: (1) metadata-only manifest and data/views + numerical tests; (2) calibration/gate/metrics and frozen encoder + focused tests/real calibration gradient check; (3) complete fixed Stage A on actual CUDA, preserve scores and apply gate; (4) only if it passes, implement/test/run Stage B. A negative scientific signal is a completed audit, not a reason to tune it.

## 2026-09-11T20:49:00Z — Implementation and setup validation

- Metadata-only manifest fixed at research_log/T004_manifest.json: 18 COCO IDs/hashes, first6 calibration and next12 evaluation; 000000057760 excluded for shorter side313. Selected files copied to TTIE/shared/t004/images. No image dataset committed.
- Natural-image/views increment: 2 tests passed in 1.623s, including exact legacy gain convention agreement and finite nonzero differentiable crop/resize gradients.
- Frozen scoring/calibration/audit increment: 5 tests passed in 1.589s; test encoder is a tiny mock for contract tests only, never presented as experimental CLIP. Tests cover frozen parameter ownership, detached gates, threshold boundary, exact inactive zero loss, calibration-only statistics, metadata-independent scores, tied AUC and literal conjunction.
- Full local suite: 34 tests passed in 12.949s, including all27 prior regressions. git diff --check passed. Real pretrained CLIP input-gradient/repeat check is included in the remote script and will run on a calibration image before held-out scoring.
- OpenCLIP2.26.1/torchvision0.19.0 installed only in TTIE venv. Existing torch2.4.0+cu121 unchanged. Upstream pretrained config confirmed bicubic/shortest, mean/std matching code; torchvision transform is shorter-side224 plus center224.
- Observed acquisition failure: A6000 host cannot reach huggingface.co (Errno101). Local Windows HEAD to the exact official checkpoint returned200 and605219813 bytes. Downloading the same checkpoint locally for checksum and transfer, with no model/source substitution and no outcome-based choice. No CLIP scores or held-out outcomes inspected yet.

## 2026-09-11T20:54:00Z — Model ready before scoring

Official checkpoint605219813 bytes downloaded locally in104.076s, transferred successfully, and remote SHA256 matches local: 1bd3c7172de5b207ceac554f5ab5266166f3b9baccc9af5989bc801016d080ad. Identity/config in T004_model_identity.json. Original failed server download exhausted retries with LocalEntryNotFoundError; no substitute weights used. Installation receipt T004_install.log retained. On remote torch/torchvision, odd-size differentiable resize/center-crop matches torchvision tensor geometry exactly (maximum difference0). No CLIP scores computed yet. Ready to deploy the fixed audit.
