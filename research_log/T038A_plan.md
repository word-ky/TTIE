# T038-A: REFERENCE_GRADIENT_DIAGNOSTIC_ONLY

Accepted T036 cohort and common trajectory only. Fixed steps0,10,20,30,40 plus original selected step:501 states. Stage A verifies all100 saved selected outputs <=1e-6 before any gradient call, computes g_E from the unchanged frozen T014 energy with accepted normalization, and freezes raw states, images, gates, active coordinate masks and gradients before any reference access. Stage B computes isolated float64 RGB-MSE g_R at those same states, with no updates/selection changes. Exact T029 active-coordinate float64 dot/cosine/norm convention and 1e-12 degeneracy handling are reused. Split legacy raw EV+gamma versus new shared gain; report all fixed steps and original selected all100/loss29/nonloss71, sign patterns and gradient norms/dots.

Gain-specific mismatch supported iff loss-selected gain median cosine <=-.25, positive-dot fraction <=.35, and legacy positive fraction exceeds gain by >=.20; otherwise gain-specific mismatch not supported / shared-or-mixed field failure. Independent standard-library dot/cosine replay of all501 states will exceed the required deterministic30-state subset and <=1e-6 tolerance. No gradient samples, loss identities, or reference outputs enter inference. No optimizer, controller, retraining, new cohort or official test. Stop after this audit.

Local focused tests2passed7.92s. Accepted renderer/energy/T029 scalar sources staged as exact Git bytes. Large Stage-A images remain remote; scalar gradients/reports are retained locally and both server roots. GPU1 preferred for both stages. T036 prior hashes checked before work; historical energy-gradient numerical drift is recorded rather than imposing an additional unrequested equality gate.
