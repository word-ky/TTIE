# T032-A execution plan

Implement the exact OPEN specification at main a0e0c481. T031 accepted merge53bda235. One source-only 95th-percentile cross-image nearest-neighbor radius, one fresh cohort, one fixed first-exit prefix selector. No tuning. GPU preferred.

Reuse map: unchanged `ttie.gamma_range_ttt.trajectory` owns the accepted T026-A 40-update path; accepted `T031A_support/core.py` owns standardized nearest-source scoring; new `ttie.support_trust` adds cross-image masking and first-exit selection only. Adapt T030 low-only runner, retaining original/guarded output filenames for unchanged T030 evaluator compatibility (`guarded` means T032 support rule, never self-reversal). Reuse accepted full-frame float64 PSNR/RGB-SSIM evaluator and independent SSIM. No donor code outside TTIE.

Baseline health: local `python -m pytest -q tests/test_gamma_range.py` 3 passed in28.55s. Existing accepted T026/T030 GPU receipts establish the real baseline path. Next increment: synthetic cross-image distance, strict > radius boundary, no re-entry, step0 exit/identity and earliest-tie tests, external radius mismatch rejection. Then source-only radius GPU computation and independent SciPy all-row replay, followed by one fresh-cohort GPU trajectory run and independent all-distance/all-decision replay. Only afterward bind complete output freeze in a separately persisted normal-deployment receipt and evaluate once.

Cohort ledger starts from existing encoded-metadata training689 records only; validation100 + T023source16 + T030/T031normal-used100 excluded, leaving473. The prior T030 exclusions/actual T023decode receipts and accepted T031 report account for reference use; earlier baseline smokes were low-only. Sort SHA256(relative_low_path + "|" + low_file_sha256), UTF-8, first100. Manifest SHA256 f0c3f024fadbfc2aa3ad6616a4ba0e89a5c56536b300877031982f3b5acc53ff. No fresh reference pixels have been opened.

Retain T026-A no-active identity bypass if encountered (zero optimizer updates), with support selection on its one actual state; this preserves accepted behavior. All active trajectories have41 states. Freeze all scores, decisions and200outputs before reference deployment. Joint qualification gate: paired mean PSNR >=+0.30dB and SSIM>=0; otherwise negative/insufficient. Stop after one cohort. D: currently nearly full; retain large outputs exclusively on server with F backup and bring back compact states/receipts only.
