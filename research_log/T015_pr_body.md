T015 is accepted by the research lead as a controlled fresh negative (4/10). This PR preserves that complete experiment and now closes the automatic source-attribution review: future launches compare actual scientific file bytes with the declared Git commit before model loading, scoring or output creation.

The small reusable guard checks an explicit 58-file allow-list against actual Git blobs and rejects mismatches, invalid/missing commits or blobs, missing runtime files and Git failures. In a worktree it also rejects uncommitted scientific changes while permitting bookkeeping changes. The pilot records the verified inventory. The shell now delegates directly to that guarded entry point; its former environment/test prelude no longer runs before the check. Full unit tests are run separately.

Closeout validation: **11 focused provenance/entry-point tests pass (26.891s); all 143 local tests pass (118.624s)**. Python compilation and Bash syntax pass. Actual committed checkout `a4041746301f3cca3550393d188f8461b7fe30a0` passes all 58 file comparisons, while historical T015 SHA is correctly rejected against the newer guarded checkout. No accepted output, metric, model, renderer, objective, trajectory or routing rule changed. No fresh evaluation or A6000 scientific job was launched.

Exact closeout code/test files: `ttie/routing/provenance.py`, `ttie/routing/pilot.py`, `scripts/run_t015_a6000.sh`, `tests/test_routing_provenance.py`. See `research_log/T015_closeout.md`, its four test logs and `T015_closeout_verification.json` for the implementation boundary and evidence. Research-owned current inbox/state were preserved from main; Codex mailbox history remains append-only.

The new guard was **not present during the historical T015 run**. That accepted run is supported by its separate retrospective 7/7 Git-blob comparison; the late named-diff record and wrong raw release metadata label remain disclosed in `T015_freeze_audit.json` and `T015_analysis.md`. The new guard prevents recurrence in future Git-backed launches. Runtime archives without accessible Git commit objects now fail closed; no remote deployment redesign is included here.

Historical scientific result: 40 fresh images ×6 primary conditions, 240 inputs, only4/10 clauses pass. Spatial routed MSE0.037806382088456304 versus best fixed Region2 0.03504357374816512 (7.88%worse); reference-only oracle0.03436451945065831 gives only1.94%headroom, below3%. Routed/oracle disagreement51/120 and MSEratio1.1001574499751097. The research lead accepts this bounded negative and does not authorize a smarter router over the same candidates.

Historical scientific source `c4e58e5ad64bfce0bea72561997db8007e12b510`; run `20260912-213014-ttie-t015-fresh-ready`; actual release `20260912-212427-ttie-t015-fresh`. Historical tests132local/132A6000; remote saved-output verification covers4560hashes,54,981,354,720imagebytes and23040energycheckpoints; local2400smallhashes/2400rows match with maxsummarydifference6.94e-18. Full small evidence and all distributions remain committed; complete pixels remain on A6000 F. ArchiveSHA256 `7b2ff9d718f946300c24872c60d132a34e9f0c3b80be2b308a28b4c3a159d9a6`.

Stop after this engineering closeout and await research-lead merge/next task. Do not self-merge or begin T016/learned-basis/detector/meta/prompt/ViT3 work.
