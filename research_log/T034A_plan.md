# T034-A isolated WB oracle plan

Main2db0a983 task; T033accepted1ad22cd7. No deployable change. Exact T028 optimizer function reused by import, same Adam.05/500updates, identity+frozenT026selected starts, full RGBMSE doubleaccumulation throughfloat32 renderer, earliestMSE winner. Add only3WBraw channels per Region2 (20raw parameters total), existing exp(log2*tanh(raw)) gains[.5,2], identity0raw; contrast stays1. Inactive WBraw reset0 and inherited Region2 output bypass remains exact. Reuse old EV/gamma projection through its raw slice; bounds/geometry/order unchanged. No proposed controller or training input.

Before oracle: fixed all100originalvalidation lows with identity and acceptedselected starts, WBidentity only, reconstruct compared to acceptedT026/T028renderer <=1e-6; no normals during this regression. Bind original split, T026 outputs/decisions/starts, T028 original freeze and per-image metrics. If identityWB regression fails, stop structurallyblocked as requested. Baseline T028 focusedtest1passed9.53s. Synthetic newtests cover mixed active/inactive semantics, WBbounds and literal optimizer-function reuse. After green, soleGPU1run100x2x500, all501rawstates/start and bestoutputs persisted, then independent postfreeze metrics and distributions against read-only T028CSV. No T028/Ours/Retinexrerun, officialtest, alteredbudget or sweep.

Use same original low/normal nativefloat32 decoding and T028 score helper; diagnostic normals only inside REFERENCE_ORACLE_ONLY. Classification: substantial iff mean deltaPSNR>=1.5 and median>=1; limited iffmean<.5; otherwise mixed. No promotion regardless of outcome. Report both-start and winningmetrics, selectedstart/step histograms, WBchannel/regiondistribution/boundhits and EV/gammaboundhits with inactiveidentity counts separate.

Ddisk nearly full: fresh checkout excludes duplicate T028history files and retains only required scripts/preflight/scalars; full oracle outputs/histories backed up onserver/F and only compact evidence retrieved. All project records remain under project root.
