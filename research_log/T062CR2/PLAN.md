# T062-C-R2 fixed fresh qualification

Authorization a557ccb68a17e03c30f579325bb916681d173993. Immutable cohort evidence52823001, SHAe8a66a350bcce5282df2e8114355afc434bcd0f4ea69e54957ebb7618f9183c2. No selection/order changes.

Reuse exact accepted T026 and T036 modules, CommonRegion2/CommonBox, low-only FixedObjective and frozen T014/CLIP/prototype/gate assets. Candidate core is the exact T062-A loop prefix,27updates; loss imported unchanged. Focused test checks bitwise equality of every state/image/component/gradient against the accepted40-step prefix, including inactive quadrants. Controls retain40active updates and minimum T014-energy earliest-tie selection. All paths reset independently from raw low.

Infer extracts and reads only100 low images from the canonical archive, hashes all sources/assets/lows, runs on physical GPU1, and freezes300 output tensor hashes plus output/trace file hashes and config/cohort/source identities. Save identity outputs for protocol-required no-adaptation comparison. Reference process can run only after inference exit0/globalfreeze; it reads only100 selected normal archive entries and produces paired metrics. Independent verifier separately reopens frozen outputs and reference bytes, computes dot-product PSNR/separable SSIM and scalar aggregate arithmetic. No metric changes, resizing or quantized output export.

Gates unchanged: meanPSNR>=2,median>0 vsT036; regressions<=29,worst>=-5.614 vsT026; meanSSIM>=-.001 vsT036. Stop after PASS/NEGATIVE/BLOCKED. An inactive-all candidate cannot satisfy accepted active loop and must stop before references, without fallback.

Observed T062-A interpreter cleanup failure is repaired only by disabling its read guard after inference processing ends; all scientific reads during adaptation remain low-only. No tuning.
