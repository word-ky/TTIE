# T069-BN development numerical audit

Reuse same-repo T069B source 3c4ce3b611918891254c71781daa80b547595429: exact T062 losses/CommonRegion2, weighted_gradients; T066A development load_inputs(False)/state_input(False), T067B fixed endpoint choices, T068B candidate_freeze. No new dependencies or external code. Prior local9/14.19s and remote9/1.58s passed; real T069B failed the sum/direct numerical tolerance and this audit specifically diagnoses that existing blocker.

Increment1: residual/tolerance/score sensitivity and repeat comparison bookkeeping tests (all12, unchanged atol2e-7 rtol2e-5). Increment2: single-process repeated CUDA float32 shared-forward component/direct gradients; optional unchanged float64 control. Reuse source/input binding and frozen table receipt flow but use a development-only artifact manifest; do not hash/read any transfer or quality artifact. No T99 or labels. Exact input/state/output identity; stored gradients at k_lambda. Original tolerance is recorded, never tuned. Aggregate abs residuals over coordinates as well as row maxima; L2-relative residuals over100rows; max/median/p95 uses NumPy linear percentile convention, fixed before execution. Counts of failed rows and coordinates. Repeat all gradient hashes and exact equality; direct-repeat tolerance sets path mismatch.

Independent verifier: independently derive endpoints with existing independent_predict/independent_choices. For each component and direct gradient reconstruct a fresh forward, repeat the float32 sequence twice, and repeat the optional float64 control. Compare all resulting gradient/output hashes exactly with primary; calculate residual/norm/score statistics independently with Python math.fsum/sqrt, numeric agreement atol1e-12 rtol1e-10; original path acceptance unchanged. No new scientific threshold. Audit100 rows then freeze, aggregate, verify. No optimizer/model fits/reference reads. A positive numerical audit does not resume T069B.
