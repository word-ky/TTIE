# T003 objective-safety diagnostic

## 2026-09-11 UTC — Acceptance, task and predeclaration

T002 accepted in PR #2 comment https://github.com/word-ky/TTIE/pull/2#issuecomment-5639304681 and merged as `8d4adfbd5f1dee26917ccec070b28e7976e252c6` at 19:00:06Z. The 19:00:25Z heartbeat received acceptance; next fetch received OPEN T003 at current main `c20fa9f`. Branch `codex/T003-objective-safety` starts there. Read the full research-owned T003 specification; do not edit inbox or PROJECT_STATE.

Reuse T001/T002 ISP/episodic Adam/input-only prior, exact synthetic generation, seeds 7/11/23, all three families and eight conditions. Only new method behavior: normalized physical correction-grid anchor and plain spatial TV. No edge-aware variant, teacher, detector, network, dataset or parameterization sweep.

Predeclared before T003 outcomes:

- Physical coarse grid normalized as EV/2 and log2(gamma, WB-R/G/B, contrast). Anchor = mean of squared normalized entries. TV = mean absolute horizontal difference + mean absolute vertical difference on the normalized coarse grid. An absent axis contributes zero; global/uniform-control render 1x1 and have zero TV. No input content/masks used in these penalties.
- Keep old adapt(image, loss_fn, ...) callable boundary; add optional lambda_a/lambda_s default 0. Record prior, anchor, smooth and total trajectories separately for every episode. Zero-weight behavior must retain T001/T002 numeric outputs.
- Exact task settings, spatial4: (0,0), (.01,0), (.1,0), (1,0), (10,0), (0,.01), (0,.1), (0,1), (.1,.1), (1,.1), (1,1). Same Adam 200 steps and lr .03 for all; global unregularized and identity references. No per-image or per-condition selection. 72 inputs x 13 variants = 936 rows, 864 reset adaptation episodes.
- Compute full matrix on **CPU on the A6000 server**, plus focused actual-CUDA parity and repeat checks. This device choice is made before outcomes: the prior T002 tiny-image CUDA sweep spent about 21 minutes on 432 episodes, dominated by small operations/diagnostic synchronization; CPU is proportionate for this twice-larger sweep. Algorithms/configuration/data remain identical. Report CPU versus CUDA scope explicitly, never call the full matrix a CUDA run.
- Global is the fixed utility reference. Utility improvement is the arithmetic mean of MSE_global - MSE_setting over the nine midtone {left_right, quadrants, smooth_gradient} inputs (three seeds each); retention divides this by the same mean for unregularized spatial4. Do not average per-example ratios or clip negative retention.
- Safety mean: mean drift of all nine undegraded inputs, equal weighting across families/seeds. Safety worst: maximum drift among those nine inputs (also report worst family-mean drift). One setting qualifies only if worst drift <= baseline worst/5 and utility retention >= .70. Report all settings and the Boolean result; no winner chosen by unlisted criteria.
- Figure representatives are fixed now: baseline (0,0), combined (.1,.1), combined (1,.1), strong anchor (10,0), plus clean/input/global reference, on seed-7 dark_structures/clean, high_key/clean, midtone/left_right. Not selected by per-image MSE. Pareto plot includes every weight setting and both mean/worst clean-drift axes against retention, with thresholds labeled.
- Full per-input CSV/JSON plus per-case component/total/gradient trajectories, region errors, normalized coarse-grid variance and TV, physical-field variance, clipping, drift, parameter counts and NaN/Inf. Representative physical grids/images/EV fields saved. Summary never replaces raw results.
- Local matplotlib 3.9.2 is available. Remote matplotlib import failed (not installed); figures using matplotlib will be rendered locally from final raw tables. Remote suite and metrics need only existing torch/Pillow; no remote environment upgrade required.

Increments: (1) normalize/penalties + adaptation component tests, zero-weight regression; (2) reuse/extend evaluator and fixed sweep/summary/visual exports; (3) full CPU tests, A6000 CPU matrix plus CUDA checks, inspect artifacts, apply fixed decision rule and report. No speculative process expansion.

## 2026-09-11 UTC — Increment validation

- Fresh T002 baseline: 20 tests passed in 9.877s (`research_log/T003_baseline_tests.txt`).
- `python -m unittest discover -s tests -p test_regularization.py -v`: 4 tests passed in 6.675s. Identity normalization/anchor zero, bounded finite values/gradients, analytic TV, global TV invariance, decomposed total equality and T001 frozen numeric MSE regression passed.
- Initial sweep API / clean-reference replacement: 2 tests passed in 6.293s. Extended suite adds a known-number test of the 5x/70% conjunction and ratio-of-means formula.
- Device checks are predeclared as exact CUDA repeat plus reported CPU/CUDA differences for all 13 variants on one fixed case. Nonsmooth TV can amplify rounding differences; do not silently relax a failed parity threshold. The full matrix uses one fixed CPU device/backend for consistent aggregate decisions.

- Complete local suite: 27 tests passed in 14.915s; git diff --check passed. Tested seven new contracts plus all T001/T002 regressions. Source ready for deployment without outcome-based configuration changes.

