T059-I auth e381a11c. Source completed remotely after local ENOSPC, before science. First G baseline aggregates/normalization checked by SHA-bound recorded evidence; exact baseline scalar recomputation deferred to evaluation because opening held targets first would violate explicit map->training-only predictions->held stage boundary. Mixed evaluation_rows full-file SHA is inherited until allowed held stage; training scalar uses selected-storage byte reads only and is checked again against whole-file SHA after prediction freeze. No gradient tensor deserialization. Source/diagnostic inference separate processes. GPU1 maps, CPU verification. No science run yet.
