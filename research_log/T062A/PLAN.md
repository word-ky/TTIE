# T062-A fixed objective control

Authorization dda0be7ec8f182aeea702c1ba0bb9727a196089a. Reuse unchanged CommonRegion2/CommonBox and low-only gate assets from T036. Adam0.03, identity12raw,40updates, min-objective earliest tie. Loss spa averages concatenated horizontal/vertical RGB pooled differences (all valid edges, no padding); exp uses nonoverlapping16cells, target0.60; col global channel means. Weights1/10/5 frozen before results. No T014 learned energy or T059/T060 method is invoked.

Baseline tests: first collection lacked tests import path; setting PYTHONPATH to repository and tests resolved it. 5 original action tests passed21.63s; 3 new loss/gradient/trajectory tests passed22.68s. One harmless scalar-detach warning occurred in a synthetic test. Standard independent NumPy objective and separate PSNR/SSIM arithmetic are used after freeze. Baseline outcome data is staged but not opened by the inference process. Model/code imports occur before installing a low-only data guard; all100 outputs, images, raw states, losses and hashes are fsynced before the separate evaluator reads any reference or baseline outcome. Inference on physical GPU1, bulk outputs on F due previous home capacity issue. No parameter sweep.
