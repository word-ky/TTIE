# T062-C literal cohort preparation

Authorization ce38725aa843cfd94aa192ca00aacc9945b8c61f. Reuse the accepted T036 train-pair inventory (689 pairs), T036 manifest exclusions, and accepted historical reference-access ledger. No image payload, per-image quality metric, official test, or cross-dataset access.

First freeze exactly the specified hash-prefix selection excluding only T036 filenames. Then check the concrete previously-unused requirement against existing access receipts. Do not silently add exclusions or select a second cohort. If prior reference use is observed, report the contract conflict before GPU inference. This is a data-preparation check, not an efficacy experiment.

Focused tests establish literal SHA ordering, 100 selection, filename exclusion, and independence from prior-use metadata. Independent verifier recomputes selection from the accepted original train inventory and rechecks actual access-receipt overlaps.
