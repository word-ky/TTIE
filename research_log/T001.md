# T001 execution log

## 2026-09-12 UTC — Repository and implementation contract

- Read all coordination files at base commit `367e3c33c28a1781d03b72aec4734be032e78b7f`; T001 is OPEN. No prior code, tests, dependencies, or project logs exist. Local checkout was empty and is now cloned from word-ky/TTIE.
- Branch: `codex/T001-spatial-isp-scaffold`. Mailbox and PROJECT_STATE ownership will be respected.
- Environment baseline: `python -c "import torch; print(torch.__version__); print(torch.rand(1))"` passed on CPU with PyTorch 2.13.0+cpu. No runnable project baseline exists.
- Reuse map: PyTorch tensor operators/interpolate/Adam for transforms, fields, optimizer; Python unittest for tests. All project-specific ISP, adaptation, and toy evaluation logic is new because the repository contains documents only. No donor code copied.
- Predeclared toy: seeded RGB periodic texture around 0.5, left multiplied by 0.45, right by 1.55, then clipped to [0,1]. Fixed 8x8 patch mean RGB prior of 0.5, no clean/reference argument or inferred clean statistics. This prior favors locally midtone, balanced-color images and is not general restoration supervision.
- Both models: identity initialization, exposure -> gamma -> white balance -> contrast, same physical bounds, Adam, 200 updates, lr 0.03. Global 1x1 grid versus spatial 4x4. Input is one float32 RGB image [1,3,H,W] in [0,1]; no batch/detector infrastructure.
- Increment 1: implement ISP and test identity, all operator gradients, analytic transformations, interpolation and global/constant-grid equivalence. Expected: finite gradients and matching semantics.
- Increment 2 after increment 1 passes: implement episodic adaptation and fixed label-free loss; test intended updates, frozen loss-module parameters, detached input, reset determinism, CPU diagnostics.
- Increment 3 after increment 2 passes: deterministic synthetic demo and evaluation-only clean access, save raw tensors, PNG comparison, full JSON diagnostics; run full suite and repeat demo to check reproducibility. Report observed advantage or failure without tuning based on clean metrics.

## 2026-09-12 UTC — Increment 1 passed; user scope additions

- `python -m unittest discover -s tests -p test_isp.py -v`: 5 tests passed in 3.027 seconds, CPU. Identity including 0/1, independent operator values and gradients, bilinear interpolation, bounded fields and global/constant-grid equality passed.
- User authorized finding A6000's wjq directory, creating a TTIE project and executing experiments there.
- Created ACTIVE thread heartbeat `ttie-chatgpt`, every 15 minutes, to read GitHub tasks/reviews and continue authorized work; user reports ChatGPT has an hourly heartbeat. No real end-to-end future wake/reply has yet been observed. No duplicate task reruns while mailbox remains OPEN; quiet when unchanged. Official scheduled-task documentation consulted at https://learn.chatgpt.com/docs/automations?surface=app.
- Using existing AutoDL PowerShell workflow at `D:/work/claude-autodl/autodl-workflow-clean`; locating and inspecting A6000 before deployment. Python 3.12.7 and Pillow 10.4.0 verified locally.

## 2026-09-12 UTC — Increment 2 passed and remote discovery

- `python -m unittest discover -s tests -v`: 10 tests passed in 12.812 seconds. Only ISP fast parameters change; callable module parameters and input gradients remain untouched, reset reproducibility passes after an intervening episode, CPU diagnostics finite, zero-step identity and analytic prior value pass.
- A6000 status succeeded through the existing workflow: hostname wenchang-PR4904W1, two RTX A6000 GPUs (49140 MiB each), both 0% utilization/15 MiB memory at inspection.
- Existing wjq directories found at `/home/liujianhua/wjq` and `/home/wenchang/asdasdsad/wjq`. Default noninteractive shell has no `python` executable (exit 127); inspecting existing interpreter path before creating this project's environment.
