# T070-A final candidate freeze/replay

Reuse-only composition, no new scientific method. Baseline: accepted T062A40step prefix and T062CR2 exact27step loop, FrozenCLIP/Prototypes/FixedObjective original global assets, T066A19D features/predict, T063C rho, T067B choices(.875). Latest accepted source a6cd7e4074521338dc3a5a9507cca89a24cf5804/evidence0296704d03b6d6c2b0fdccf2c9d5582d8660fad7. Prior remote11passed2.72s, independent1000forwards exact endpoints. No dependencies or licenses changed; all donor code same repository.

FinalOurs(manifest).__call__(low) consumes only degraded RGB tensor and loaded frozen globals. Uses exact CLIP/prototype/gate assets (no energy/baseline model), identity initialization, accepted27updates float32CUDA prefix, low-only weights1/10/5, fixed19D features and T066A frozen probability model. Select exact first predicted-safe and normalized progress interpolation .875; no new fallback. Operational CLI --low/--manifest/--out only.

Manifest has exact scientific source SHA, source/module binding, clip/prototype/gate and probability-model hashes, all constants including seed7, Adam.03/defaults,27updates, TF32off, native RGB preprocessing, GPU/device and package versions. Create immutable canonical bytes after source commit and before data replay; verify source and resource hashes once at load and stable manifest hash throughout. Source commit refers to wrapper implementation; the later manifest/evidence commit is separate (no circular self-hash).

Audit reruns adaptation from image for all200 images (100development then100exposed transfer), compares all28states,27prebox proposals/gradients,loss components/values,gate/bounds and output hashes to accepted trace prefix; exact selected step/state/output to original T067B candidate freeze(.875) and T067C choice freeze. Predeclare first5development+first5transfer for a second complete inference:210optimizer runs,5670updates; globalmodel fits0. No quality/reference artifacts in bindings or reads. Persist hashes/receipts, not large image trajectories. Stop on any mismatch, do not change scientific settings.

Independent verifier uses bound stored trace prefix, rerenders28states for each low image, independently computes fixed selection (independent_predict/independent_choices), checks trajectory and selected hashes against audit and original anchors, verifies repeat table and manifest; optimizer runs0 in verifier. It does not trust primary choices/output hashes. Primary reruns complete gate/optimizer, verifier separately confirms target-free anchors. Reuse existing T066A ReadScope for actual data read audit (no new governance layers).

Increment1: frozen API/selection composition tests and imported baseline tests. Increment2: manifest/remote environment freeze then full200+10GPU replay and independent5600GPUrenders. Final focused suite and end-to-end evidence. No PSNR/SSIM/clean/labels/baseline outcome data/final datasets. Stop at freeze result; no heldout evaluation.
