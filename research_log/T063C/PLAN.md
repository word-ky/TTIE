# T063-C normalized objective-progress stopping audit

Exact task: authorization.md. Reuse T063-B data boundaries, frozen trajectory renderer, calibration envelope, reference evaluator and independent verification structure; only replace the statistic/selector and candidate rule. Recompute exact accepted T062 components and float32 GPU total dot product, checking saved values. Use binary64 L_best/D/q and threshold arithmetic; earliest finite L crossing, zero-progress fallback0. Candidate set exactly finite clipped development progress values plus0/1. Freeze chosen rho, all development steps, source/cohort hashes and table before target-free transfer selection. This rule needs the full27-step trajectory and does not establish compute savings.

Baseline health:6 T062A/T063B tests pass30.74s. New rule:4 tests pass15.79s (earliest nonmonotone crossing, zero progress, exact clipped grid, direct loss threshold arithmetic, safety and smallest-rho tie). Next: affected regression and sole GPU run, then independent GPU reconstruction and scalar progress/grid/selection plus independent metrics. The previous 344-file binding and accepted development quality remain reusable; no transfer quality/oracle access before global choices/output freeze. No optimizer, alternate statistic, new cohort or held-out access.
