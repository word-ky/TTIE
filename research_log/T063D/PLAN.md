# T063-D fresh qualification

One predeclared fresh cohort from accepted T062-C-R1 inventory: prior416 reference-used plus100 subsequently exposed, leaving173. Later branch snapshot shows only R1,R2,T063A/B/C new; all earlier snapshot branches unchanged. R1 is metadata only; R2 explicit100 normal reads, A same100, B/C original development100 plus same100. Later per-image/access records are bound and projected to paths; no later new normal path. Historical204 source bindings are rechecked. Fresh selection first100 SHA256(T063D:+relative lowpath); independent binary-digest reconstruction checks516 exclusions, zero previous/development overlap.

Reuse exact T062CR2 27-step trajectory plus frozen T063C.choose at rho=.9857470621423519; controls unchanged accepted T026/T036. No retuning or source-field reuse from prior outcomes.8 baseline tests pass11.72s;2 cohort/trajectory integration tests pass9.05s. All300 outputs and100 selected steps globally freeze before references. Independent verifier checks cohort/source/assets, all2800 candidate states and objective progress,200 selected control states, output hashes and independent metrics/gates.

PR142 feedback4056128505/4056128506 is addressed in the fresh execution boundary: independent PNG low inputs, no prior outputs.pt container loads, no reference manifest in inference bindings. Low extraction is a separate process opening only Low ZIP entries. Evaluation-only manifest/source provenance/reference hashes are verified after the global output freeze and reference-open marker. Existing accepted T063-C source/evidence remains immutable; no rerun/relabeling of that accepted audit.

No second cohort, rho, heuristic, official test/cross-dataset access or compute-saving claim. Report the frozen gates then stop.
