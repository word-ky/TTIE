# T005 counterfactual relative CLIP signal

## 2026-09-11T21:49:00Z — Receipt and fixed predeclaration

T004 accepted at https://github.com/word-ky/TTIE/pull/4#issuecomment-5640910270 and merged as8c9680c56ced11d4433908091a4e4575ca04ab1f. Heartbeat received OPEN T005 on main e2b7b30. New branch codex/T005-relative-clip. Read complete inbox, state/protocol/outbox and project handoff. Baseline34tests passed in11.650s (T005_baseline_tests.txt). Reuse implement-method-from-baselines and existing AutoDL workflow already read; no new team/delegation.

Before any T005 score: research_log/T005_manifest.json fixes30COCO images from the same existing200-image image-only pool, excludes all18T004 IDs, metadata-sort remaining IDs and require original minimum side>=320. First10calibration/next20evaluation; zero overlap with T004. No annotations or previous pool scores inspected. All selected images copied to TTIE/shared/t005/images; manifest includes IDs/dimensions/SHA256/source URLs/exclusions. This is fresh relative to T004, but the original200-image selection provenance remains incompletely documented; do not claim representative sampling. No image dataset committed.

Reuse frozen ViT-B-32/laion2b_s34b_b79k, exact nine T004 prompts/text prototypes, model hash1bd3c7172de5b207ceac554f5ab5266166f3b9baccc9af5989bc801016d080ad, open_clip_torch2.26.1/torchvision0.19.0/torch2.4.0+cu121. Same RGB shorter-side320 preprocessing, five differentiable full/quadrant views, shorter-side224 center224 CLIP geometry and normalization. Same six exposure conditions. Existing natural.py/FrozenCLIP/calibrate/decisions/audit_group own these behaviors; add only relative probes and task-specific orchestration/gate.

Fixed delta_EV=.25. Probe current pixels by multiply2**(+.25) or2**(-.25), clamp[0,1], then use identical view crop/resize. Cropping commutes with pixelwise exposure/clamp, so this produces the prescribed raw-view counterfactuals before CLIP resizing. Compute original/plus/minus scores; r_dark=d_dark(original)-d_dark(plus), r_bright=d_bright(original)-d_bright(minus). Preserve all six absolute score terms, both responses, winner, activation, fixed corresponding probe target, and input/probe clipping. Pixel-only response/gate/target API accepts no clean/condition/mask/label metadata.

Calibration: only10clean calibration images x5views. Per-response linear95th percentile, population std (correction0) floored at.01. Winner argmax(r_dark,r_bright), ties dark; strict greater-than winner threshold. Persist constants before held-out scoring. For absolute-score reference on this fresh split, preserve T004 calibration thresholds/scales exactly, never refit; AUC/paired statistics independently use corresponding scores. No threshold/probe/model/prompt/crop/split changes after held-out outcomes.

Stage A contains50calibration rows +600held-out rows (20images x6conditions x5views). Reuse tie-aware pairwise AUC and strict paired-increase definitions. Summarize relative FPR/AUC/anyTPR/correctTPR/active-type precision/paired changes with all/full/quadrant breakdown. Preserve absolute baseline metrics on the same fresh images using frozen T004 calibration. Gate on all100held-out clean views and each100homogeneous-degraded views: FPR<=.15; both response AUC>=.75; each correct-type TPR>=.30; combined correct type among active homogeneous-degraded views>=.80. A zero-active combined precision is undefined and cannot pass. Paired-increase is reported, not an extra gate. No mixed-view true type invented.

If Stage A fails, stop T005 with all raw rows; do not implement/run Stage B or learned prompts/T006. If it passes, only then implement/test explicit EV/Gamma mask, exact-inactive identity and fixed detached probe targets. Stage B fixed Adam40/lr.01, global1x1/spatial4x4, old bounds, no0.5prior in semantic loss; compare identity, full-gate global probe-only, quadrant-gated bilinear2x2 spatial probe-only, relative-TTT global/spatial and easy old-prior references. No per-image selection or optimizer tuning from held-out metrics. Panel cases will be fixed by manifest order before any adaptation. Do not infer correction quality from lower CLIP loss.

Increment plan: (1) fresh manifest + exact pixel probes/calibration/metric unit tests; (2) full regression, commit protocol, actual CUDA Stage A once; (3) gate decides whether adaptation implementation is authorized. Scientific negative is a completed result, not an invitation to tune.

## 2026-09-11T21:52:00Z — Implementation ready before scoring

Fresh manifest has30images and no T004 overlap. Four new focused tests passed in1.677s: exact probe multiplier/sign/clamp/response/target formulas, metadata-independent scores/gates/targets, calibration exclusion and fresh split, literal correct-type TPR/precision conjunction including no-active case. Full38tests passed in10.246s, including all34previous regressions. No model or earlier ISP/scoring/data code changes. Exact commands: python -m unittest discover -s tests -p test_relative_clip.py -v; python -m unittest discover -s tests -v. No T005 CLIP score inspected. Fixed protocol, manifest and all code will be committed before deployment; actual CUDA Stage A will use scripts/run_t005_a6000.sh.

## 2026-09-11T21:54:00Z — Remote launch recovery

Source b1f7e63e26af574df9a53de1ba288252a9e84968; release20260912-055303-ttie-t005; run20260912-055307-ttie-t005-a6000. SSH disconnected after run script creation but before tmux launch. Verified no matching experiment process/session/train.log. Started the existing generated run.sh in its intended tmux session via workflow helper, retaining same run ID/configuration. T005_launch_failure.txt preserves exact failure and recovery. Other GPU jobs unchanged. No scores had run before recovery.

## 2026-09-11T21:57:00Z — Stage A completed, negative verdict

Run20260912-055307-ttie-t005-a6000 finished21:54:29Z with exit0; tested source remains b1f7e63e26af574df9a53de1ba288252a9e84968, release20260912-055303-ttie-t005. Remote38tests passed in5.063s. Actual CUDA frozen-CLIP audit ran once after the launch transport recovery; no experiment restart, model change or outcome-fed patch. No new dependency/model download. Calibration50rows and heldout600rows make650unique finite rows; response reconstruction from stored original/probe scores has maximum absolute error0. Manifest has zero T004-ID overlap. All raw terms and clipping/targets retained.

Frozen relative calibration: tau_dark=.011369550973176951; tau_bright=.005873285233974456; scales=(.01,.01), both at the predeclared floor. Only10clean calibration IDs/50views used. These constants were persisted before held-out scoring; unchanged absolute T004 calibration also preserved in config.json.

Literal gate: only clean FPR passes. FPR7/100=7%<=15%; dark response AUC .5806<.75; bright response AUC .2945<.75; correct-type TPR dark10/100=10%<30%, bright5/100=5%<30%; combined active-type precision15/21=71.429%<80%. Thus Stage A fails five of six explicit checks. Stage B not authorized/run: no EV/Gamma-mask changes, adaptation episodes, probe-only correction baseline, reconstruction metrics or panels. Stage-B-only identity/mask/probe-only/adaptation leakage/reset tests are unimplemented/unrun, not counted among38passes. Pixel-only score/gate/target and existing adaptation regressions do pass.

Relative any-activation TPR dark10%, bright11%; correct type among active dark100%, bright45.455%. Paired corresponding-response increase dark56%, bright29%. Full-view relative AUC dark.52/bright.40; quadrant .597344/.270625. Full-view dark has zero active views, so its active-type precision is undefined, not zero or100%. The aggregate gate is unchanged by subgroup statistics.

Absolute reference on the SAME fresh T005 split: AUC dark.8339/bright.6040, paired increase100%/85%, FPR8%, any-activation TPR29%/9%, correct-type TPR28%/7%, combined active-type precision35/38=92.105%. T004 thresholds/prompts/model were not refit. Same-split comparison is decisive: the prescribed relative correction response worsens both AUCs rather than rescuing the absolute signal. High paired absolute exposure sensitivity does not imply that its local correction-response magnitude ranks degradation severity. This does not establish failure of all relative signals or isolate saturation/clipping as the cause. No response-sign inversion, magnitude/percentile/prompt/crop/model tuning after outcomes.

Complete all/full/quadrant metric tables are artifacts/audit/summary.md and summary.json; scores.csv/json retain650rows with original/plus/minus scores, responses, relative/absolute gates/types, detached target and clipping. calibration_scores.json contains the50pre-evaluation rows, calibration.json the frozen constants, config.json the full manifest/model/prompts/probe/absolute constants, and audit_receipt.json the row/split/finite/formula checks. Execution meta/run/train, environment and38-test receipt are in research_log/remote_runs/20260912-055307-ttie-t005-a6000/. Launch transport failure preserved in T005_launch_failure.txt. No model weights/image data committed. No active TTIE job remains.

Recommend research-lead review of this negative fixed relative-signal audit. A separately scoped learned/source-trained degradation signal may be considered by the lead, but no learned prompts/T006 started. The20held-out source images have correlated crops/conditions and come from an incompletely documented200-image pool; no population, restoration, detector or spatial-TTT benefit claim.
