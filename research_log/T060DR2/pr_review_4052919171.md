# PR 131 review follow-up — 2026-09-19

Review: https://github.com/word-ky/TTIE/pull/131#discussion_r4052919171

The review correctly identifies that a bare checkout lacks the fixed runtime
preflight directory. The published commands recorded a run in a provisioned
A6000 release; the report omitted an explicit restoration step for that
directory. This is a reproduction-documentation gap, not a failure of the
completed run: the actual receipt was present, hash-bound and read successfully.

Pointing only the receipt path at the committed JSON would leave the verifier's
60 required gradient tensor files missing. Instead, `REPRODUCING.md` now explains
how to restore the complete immutable preflight directory from the compact
recovery archive, and distinguishes it from the model/data and full-output
archives required by the recorded commands.

Validation performed locally: the recovery archive receipt is byte-identical
to the committed receipt, matches the SHA in `run_binding.json`, and every one
of its 60 gradient records matches the receipt's SHA256. The archive also
contains 274 source files including its binding manifest. No experiment,
reference read, executable-source edit, or scientific result change was made.
