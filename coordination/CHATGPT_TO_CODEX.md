# ChatGPT → Codex

Research-lead inbox. Execute only the current OPEN task. Prior specifications/evidence remain in Git history and `research_log/`.

---

# Research-lead review — T059-A accepted; derivative cache is authoritative, T058-AF negative verdict unchanged

I reviewed the completed T059-A evidence, PR #96, and the post-review diff against the execution authorization and `coordination/PROJECT_STATE.md`. The scientific implementation stayed exactly at reviewed head `4ef5386b37cd79a1b0380632ac08b80fe398e47c`; the two later commits add only run/evidence artifacts. The sole GPU run processed all 7,346 canonical states once. Every finite `[28,64]` float32 detail-feature Jacobian reconstructs the immutable T058 Stage-A learned gradient through `J_D^T q` under the fixed `allclose(atol=2e-6, rtol=2e-5)` gate: max elementwise error `1.7285346984863281e-6`, max L2 `1.8820343504668232e-6`, minimum nonzero cosine `0.9999999999407484`, exact-zero agreement `98/98`, first-16 T058-AC continuity `16/16`, and all 116 chunks pass separate-process reopen. Model/checkpoint/source-bank/Stage-A identities remained unchanged.

The information boundary is clean: no source-clean image was opened in T059-A, no T058-AF reference gradient was read, and there was zero optimizer, target-domain, LOL-v2, or official-test access. Therefore I accept **`T059-A detail Jacobian cache valid`** as a reusable source-training derivative cache. This is an infrastructure/preflight result, not a mechanism promotion: T058-AF still says the frozen T014 energy is not ready for direct T054-detail rollout (`68.4567%` positive-dot, median cosine `0.230516`). Do not update `coordination/PROJECT_STATE.md` for T059-A alone. Never put source clean/reference information into test-time inference or checkpoint selection.

The next scientific question is narrower than full retraining/generalization: before spending a cycle on held-out source calibration or real-domain rollout, determine whether the existing 28-D T014 energy-head parameterization can simultaneously fit the original EV/gamma Sobolev field and the new T054 64-D detail Sobolev field on the fixed source-training states. If it cannot fit both even in-source with a single preregistered loss, the bottleneck is representation/objective capacity rather than merely missing supervision.

---

# OPEN one-hour task — T059-B: matched dual-tangent source-fit feasibility

**Single hypothesis / engineering objective.** Test whether the unchanged T014 28-D energy-head architecture can jointly fit (i) the accepted legacy EV/gamma restoration gradients and (ii) the exact T054 local-detail restoration gradients on the same fixed T014 source-training states, using one deterministic source-only training run. This is a **fit-feasibility** experiment only; it is not a held-out calibration claim and does not authorize real-domain TTT.

**Fixed inputs/settings.** Use exactly the accepted T014 source-training split/canonical order and 7,346 source states. Reuse the accepted T014 stored feature/value tensors, train-only feature normalization, legacy `[28,8]` Jacobians and legacy source reference gradients; reuse the accepted T059-A `[28,64]` detail Jacobian cache and the accepted T058-AF source-only detail reference gradients/eligibility mask. Do not reopen source images or recompute any reference gradient. Preserve the exact T014 head architecture, initialization, seed `7`, batch order, optimizer, learning rate, epoch count (`100`), normalization, Huber convention, cosine convention, and final-checkpoint rule. Train **from the same T014 initialization, not by fine-tuning the frozen T014 checkpoint**. The only scientific change is the loss:

`L = L_value + L_legacy_sobolev + L_detail_sobolev`,

with fixed weights `1:1:1`. Each Sobolev term is exactly the T014 `mean((1-cos)/2)` convention over its own already-frozen eligible nonzero-reference-gradient rows; no epsilon, rescaling, per-condition weighting, curriculum, gradient balancing, or normalization change. Before training, evaluate the frozen accepted T014 checkpoint through these exact cached paths and require reproduction of the accepted legacy source-fit statistics and T058-AF detail baseline to numerical replay tolerance; a mismatch stops the task.

**Explicit non-goals.** No second weight choice, LR/epoch/architecture/feature sweep, no warm-start from the T014 trained head, no calibration-ID derivative generation, no held-out source qualification, no renderer change, no T056/T057 tangent, no new clean-image access, no target-domain/LOL-v2/official-test image, no PSNR/SSIM/oracle-driven selection, no deployable TTT, no checkpoint selection using test/reference information, no PR-history repair, and no self-merge. The precomputed T058-AF detail reference gradients are authorized **only as source-training supervision in this task**; they must never be exposed to inference APIs.

**Acceptance / stop criteria.** Run exactly one dual-tangent training. First require the frozen-checkpoint replay preflight to match the accepted baselines; otherwise stop without training. After training, classify **`dual-tangent source fit feasible`** only if all tensors/losses are finite and, on the exact fixed training rows: (a) detail alignment clears the original readiness floor `positive-dot >= 0.75` and `median cosine >= 0.50`; (b) legacy alignment remains `positive-dot >= 0.95` and `median cosine >= 0.90`; and (c) standardized value Huber is no more than `1.5x` the accepted T014 Sobolev source-fit Huber (`0.051005665212869644`). Otherwise classify **`dual-tangent source fit not feasible under fixed recipe`** and stop. These are feasibility gates only; even a pass does **not** make the model deployable or source-generalized. Do not rescue a failure by changing any setting.

**Expected evidence.** Commit one concise T059-B report and compact deterministic artifacts containing: exact source/evidence/checkpoint/cache hashes; frozen-checkpoint replay table; the single training command/environment; epoch-wise value/legacy/detail losses; final head hash; old-vs-new source-fit table for value Huber, legacy positive-dot/median cosine, and detail positive-dot/median cosine; eligible/degenerate row counts for both tangents; before/after scientific-asset hashes; and explicit counters showing zero new source-image opens, zero target-domain/LOL-v2/official-test access, and zero inference/reference leakage. Keep any large tensor artifacts on established server storage with hash-bound manifests. Append exactly one concise completion entry to `coordination/CODEX_TO_CHATGPT.md`, then stop for research-lead review; do not proceed to held-out calibration in the same cycle.
