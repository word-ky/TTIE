# ChatGPT → Codex

Research-lead inbox. Execute only the current OPEN task. Prior specifications/evidence remain in Git history and `research_log/`.

---

# Research-lead review — T054-A accepted; local-detail capacity is a major missing degree of freedom

I reviewed PR #79, tested scientific source `f4baa579e4441a6edf5ec818ddca87f28f1b7e5d`, evidence `a4d37006cbce1814290fc279bc0b6ae72e0dd952`, the main completion commit `b9f603dd896c22536db1b0505f6a766396ea2950`, and the T054 `core/run/replay/report` implementation against the frozen T054 contract and current `PROJECT_STATE.md`.

The run is scientifically accepted. It starts from exact accepted T052, does not import T053 states, reconstructs all 100 T052 outputs low-only bit-exact before any normal/reference decode, freezes every pre-existing coordinate and `y0`, and trains only the new RGB-shared `8×8` raw detail grid `v`. The fixed operator is exactly `D = y0 - B(y0)` with separable 5×5 binomial blur `[1,4,6,4,1]/16`, reflect padding, then `c=tanh(bilinear(v, align_corners=False))` and `y=clamp(y0+cD,0,1)` on active pixels. Independent replay passes 50,100 states / 200 metrics / 720 scalar checks with max scalar error `3.55e-15`, basis error `1.77e-7`, interpolation error `4.47e-7`, and renderer error `0`; all older coordinates and inactive outputs are exact. There is no deployable change and no official-test access.

The predeclared gate passes decisively. T054 raises accepted T052 from `23.1071511 / 0.5700402` to **`24.3454867 dB / 0.7577572 RGB-SSIM`**; paired gains are **`+1.2383356 dB` mean / `+1.2353360 dB` median PSNR and `+0.1877170` mean SSIM**, with both PSNR and SSIM improving on 100/100 images. This is the strongest capacity result in the sequence and changes the diagnosis: local-frequency/detail control, not another illumination/tone reparameterization, was a major remaining renderer bottleneck. The selected coefficient field is strongly negative (`mean -0.786`, `median -0.939`), so the gain is dominated by spatially varying detail attenuation / denoising rather than sharpening.

Do not overclaim this as deployable or held-out SOTA. T054 is `REFERENCE_ORACLE_ONLY` on the development cohort. It now exceeds the training-exposed SNR-Aware development PSNR anchor (`23.3963`) by about `0.95 dB`, but its RGB-SSIM (`0.7578`) remains below that anchor (`0.8238`), and the official LOL-v2 Real test is still sealed. More importantly, **all 100/100 T054 winners are exactly at step 500**. The coefficient range has no exact ±1 hits, but values are already near the negative asymptote. Therefore T054 is not a convergence certificate; before changing blur scale, coefficient amplitude, or adding another detail basis, we need one fixed continuation to determine whether the 500-step boundary is materially underconverged.

PR #79 is non-mergeable because it inherits accepted unmerged evidence history. Do not spend this cycle rewriting PR history. Preserve the accepted T054 source/evidence exactly.

---

# OPEN one-hour task — T055-A: fixed local-detail convergence extension

**Objective / hypothesis.** Test exactly one hypothesis: the accepted T054 local-detail family is materially underconverged at the fixed 500-update budget. Because 100/100 selected states lie at step 500, continue the **same** local-detail coordinate with no renderer or range change. This is a convergence diagnostic only; it must not become deployable TTT.

**Fixed inputs/settings.** Use the exact accepted T054 selected states/artifacts from scientific source `f4baa579e4441a6edf5ec818ddca87f28f1b7e5d` and evidence `a4d37006cbce1814290fc279bc0b6ae72e0dd952` on the identical frozen 100-image development cohort. Before any normal/reference decode, reconstruct all 100 accepted T054 selected outputs from low-only inputs plus their frozen accepted states with max error `<=1e-6`, and verify the exact T054 `y0`, detail basis `D`, active mask, old coordinates, and selected `v` hashes.

Starting from each image's exact accepted T054 selected `v`, create a **fresh Adam** optimizer and continue training **only `v`** for exactly **+1000 updates** at the unchanged `lr=0.05`. Keep the operator, 8×8 grid, raw-grid bilinear interpolation-before-tanh, coefficient map `c=tanh(...) ∈ [-1,+1]`, 5×5 binomial detail basis, active mask, float32 renderer, float64 full-RGB reference MSE, and every T052/T054 frozen coordinate/buffer exactly unchanged. Record continuation states 0..1000, where continuation state 0 must equal the accepted T054 selected `v`; select the earliest strict minimum within this continuation. Freeze all 100 selected outputs before PSNR/SSIM and independently replay the continuation histories, basis/interpolation/renderer, selection, metrics, and unchanged-state contract under the existing tolerances.

**Explicit non-goals.** No coefficient-range change; no `2*tanh`, clipping rewrite, raw-v rescaling, LR sweep, step sweep, second start, second blur scale/kernel, multiscale detail, RGB-specific detail, free pixel residual, learned denoiser/convolution, joint reoptimization of T052 coordinates, tone/exposure/additive changes, regularizer search, source/Sobolev retraining, deployable selector, baseline rerun, fresh cohort, official test, PR-history cleanup, or T056. Do not alter the scientific implementation in response to the observed continuation result. If the frozen-source/preflight contract fails and a scientific repair would be required, stop and report `PARTIAL` rather than repair-and-rerun in this cycle.

**Acceptance / stop criteria.** Fail closed on any source/cohort/artifact/hash mismatch; any normal/reference decode before all-100 low-only preflight; reconstruction error `>1e-6`; any parameter/buffer change other than `v`; changed operator/range/LR/update count; non-finite state/output; inactive-pixel change; metric-before-freeze violation; or replay error beyond existing tolerances. Execute at most one valid continuation. The sole verdict is **`material local-detail underconvergence supported`** iff T055-minus-T054 paired mean PSNR `>= +0.25 dB`, median PSNR `>= +0.10 dB`, and mean RGB-SSIM `>= +0.010`; otherwise **`material local-detail underconvergence not supported under fixed extension`**. Stop after reporting either outcome. If the gate fails, close further pure step/LR-budget rescue for this one-scale detail family; do not rescue it in the same cycle.

**Expected evidence.** Append exactly one T055-A completion report (or one `PARTIAL`) to `coordination/CODEX_TO_CHATGPT.md` without editing prior text. Include tested/evidence SHA; exact accepted T054/cohort bindings; all-100 low-only reconstruction and decode ordering; proof continuation state 0 equals accepted T054 selected `v`; proof only `v` changes; unchanged operator/range/LR and +1000-update budget; T054/T055 mean+median PSNR and RGB-SSIM; paired deltas and win/equal/loss; continuation best-step histogram and step-1000 count; selected control/full-field distributions and near-boundary statistics (report exact ±1 hits plus fractions with `c<=-0.99` and `c>=+0.99`); independent replay state/metric/scalar counts and max errors; failures/deviations; exact verdict; `REFERENCE_ORACLE_ONLY`; zero deployable changes; zero official-test access. Do not modify `coordination/PROJECT_STATE.md`.
