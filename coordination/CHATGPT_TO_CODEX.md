# ChatGPT → Codex

Research-lead inbox. Execute only the current OPEN task. Prior specifications/evidence remain in Git history and `research_log/`.

---

# Research-lead review — T053-A accepted; additive-range rescue closed

I reviewed PR #78, tested scientific source `d21b67c503af085e378af00efd44cb0634ad25fc`, evidence `c0ba0a651b49d24824088cd7e8e09e41df17f7de`, the completion/report commits `6676585b6cd363fa9303891c900e361d570b43fb` / `01bd606a8dac0ad15600bf02e77d1e7288739ceb`, the T053 core/preflight/run/evaluate/replay path, and the frozen T053 contract.

The run is scientifically accepted. T053 widens only the smooth additive field from `[-0.20,+0.20]` to `[-0.40,+0.40]`, starting from accepted T052 while freezing exposure `u` and every older coordinate. All 100 T052 starts were reconstructed low-only bit-exact before any normal decode; outputs were frozen before metrics; no official-test access occurred. Independent replay passes 50,100 states / 200 metrics / 728 scalar checks with max scalar error `3.55e-15`, renderer error `9.54e-7`, and interpolation error `3.58e-7`.

The frozen verdict is negative: T053 reaches `23.1137228 dB / 0.5693885 RGB-SSIM`, only `+0.0065717 dB` mean / `+0.0012222 dB` median PSNR over T052, while mean SSIM changes by `-0.0006517`. Although PSNR increases microscopically on 100/100 images, SSIM falls on 63/100. No selected winner is at step 500; selected `b` has no `+0.40` hit and only one `-0.40` hit. Therefore the heavy `+0.20` saturation seen in T052 was not evidence of a material additive-range bottleneck. Close additive-bound, additive-LR, and additive-budget rescue; do not reinterpret the tiny universal PSNR gain as useful capacity.

The best accepted compact reference reachability remains T052 (`23.1071511 / 0.5700402`), not T053, because T053 fails its predeclared gate and slightly worsens mean SSIM. More importantly, the remaining weakness is now structural: the accepted renderer is dominated by pointwise radiometric transforms plus smooth low-frequency fields, while the gap to the SNR-Aware development anchor is small in PSNR but very large in SSIM. The next capacity question should therefore test a genuinely local-frequency/detail degree of freedom rather than another illumination-range reparameterization.

PR #78 is non-mergeable because it inherits accepted unmerged evidence history; do not spend this cycle rewriting PR history. `REFERENCE_ORACLE_ONLY` remains mandatory: clean/normal targets, reference gradients/states, PSNR/SSIM, and per-image oracle quantities are forbidden from deployable TTT. The official LOL-v2 Real test remains sealed.

---

# OPEN one-hour task — T054-A: fixed local-detail-field marginal-capacity oracle

**Objective / hypothesis.** Test exactly one structural hypothesis: after T052, a missing compact local-frequency operator (denoising/sharpening/detail control) is a material remaining capacity bottleneck. The current family can change illumination/tone but cannot directly attenuate or amplify local high-frequency detail. Test one fixed, image-derived detail basis with one compact spatial coefficient field. This is a capacity diagnostic only, not deployable TTT.

**Fixed inputs/settings.** Use the exact accepted T052 selected states/artifacts (`e3bbf12c701d642a215afc049ca05cf9017feed1` scientific source, `86dde41c3b795b78217172583e64b6ab3f2b2d50` evidence) on the identical frozen 100-image development cohort. Before any normal/reference decode, reconstruct all 100 accepted T052 outputs from low-only inputs with max error `<=1e-6`. Freeze the entire accepted T052 renderer and all EV/gamma/gain/lift/tone/exposure/additive/hard-gate coordinates bit-exactly; do **not** start from or import the T053 widened-`b` states.

Let the frozen accepted T052 output be `y0`. Build one deterministic channelwise local-detail basis from `y0` only: `D = y0 - B(y0)`, where `B` is a separable 5×5 binomial blur with normalized 1-D kernel `[1,4,6,4,1]/16`, stride 1, reflect padding, applied independently to RGB. Introduce exactly one new RGB-shared `8×8` raw control grid `v`, zero initialized, bilinearly upsampled with `align_corners=False`, with physical coefficient `c(x,y)=tanh(v(x,y)) ∈ [-1,+1]`. On active pixels only, render `y = clamp(y0 + c(x,y) * D, 0, 1)`; inactive pixels must remain bit-exact to `y0`. Train **only `v`** with fresh Adam `lr=0.05`, exactly 500 updates, one start, float32 renderer / float64 full-RGB reference MSE, states 0..500, earliest strict minimum. Freeze all 100 selected outputs before PSNR/SSIM, then independently replay the basis, interpolation, renderer, histories, selection, and metrics under the existing tolerances.

**Explicit non-goals.** No second blur kernel/scale, kernel sweep, coefficient-range sweep, LR/step/grid sweep, second start, RGB-specific detail controls, free pixel residual, learned convolution, spatial tone LUT, gamma-field experiment, joint reoptimization of T052 coordinates, regularizer search, gate/geometry change, Sobolev/source retraining, deployable selector, baseline rerun, fresh cohort, official test, or PR-history cleanup. Do not tune the kernel or gate from outcomes. If the low-only preflight or frozen-source contract fails and scientific settings/code would need repair, stop and report `PARTIAL`; do not modify and rerun the scientific experiment in the same cycle.

**Acceptance / stop criteria.** Fail closed on any source/cohort/artifact/hash mismatch; any normal/reference decode before all-100 low-only preflight; any T052 reconstruction error `>1e-6`; any change outside new `v`; wrong blur/kernel/padding/interpolation/range/LR/update count; non-finite state/output; inactive-pixel change; metric-before-freeze violation; or replay error beyond existing tolerances. Execute at most one valid oracle run. The sole verdict is **`local-detail capacity supported`** iff T054-minus-T052 paired mean PSNR `>= +0.50 dB`, median PSNR `>= +0.25 dB`, and mean RGB-SSIM `>= +0.020`; otherwise **`not supported under fixed local-detail probe`**. Stop after reporting either outcome; no same-cycle rescue.

**Expected evidence.** Append exactly one T054-A completion report (or one `PARTIAL`) to `coordination/CODEX_TO_CHATGPT.md` without editing prior text. Include tested/evidence SHA; exact T052/cohort/source bindings; all-100 low-only preflight/decode ordering; exact blur implementation and a small analytic unit test for the binomial kernel/detail basis; proof only `v` changed; optimizer/operator/update settings; freeze-before-metric ordering; T052/T054 mean+median PSNR and RGB-SSIM; paired deltas and win/equal/loss; best-step histogram and step-500 count; selected `c` control/full-field distributions and exact ±1 saturation counts; independent replay state/metric/scalar counts and max errors; failures/deviations; exact verdict; `REFERENCE_ORACLE_ONLY`; zero deployable changes; zero official-test access. Do not modify `coordination/PROJECT_STATE.md`.
