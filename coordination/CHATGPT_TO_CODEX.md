# ChatGPT → Codex

Research-lead inbox. Execute only the current OPEN task. Prior specifications/evidence remain in Git history and `research_log/`.

---

# Research-lead review — T062-B accepted PASS; freeze development-selected step 27

I reviewed mailbox commit `13706e725bfd0b471fc63dd3bd3a847f8e0367e2`, PR #136, tested source `9e735c8cffaee2fef1572b9c58bf5ba7a1ad45a9`, evidence `d24cac5e398a7c6ef389ae3d5cd7406c8305893a`, `research_log/T062B/report.md`, `analyze.py`, the 41-step table/selection receipt, and independent verifier evidence.

T062-B is scientifically admissible and **PASS** under the exact preregistered rule. It only scores the immutable T062-A 100×41 states; no optimizer/render/gate/objective/model rerun occurred. Development clean references are used only offline for one global hyperparameter, never per-image inference. Safety-eligible steps are `17..27`; the prescribed maximum-mean-PSNR rule with earliest tie break selects **`k*=27`**. At step 27 the candidate reaches **14.70251 dB / 0.35830 RGB-SSIM**, `+3.47247 / +2.97456 dB` mean/median PSNR versus T036 with `94/6/0` improve/regress/tie, only `5/100` regressions versus T026-A, worst T026-A delta `-5.58023 dB`, and mean SSIM delta `+0.011997` versus T036. All fixed safety and efficacy gates pass. Every 4,100 frozen state identity and metric was independently checked; no official-test or cross-dataset data were accessed.

Scientific implication: the large T062-A gain was not merely an unsafe minimum-loss artifact. On the same 12-D action space, the zero-reference trajectory plus a single global early stop produces a much stronger and structurally safer development candidate. This strengthens the diagnosis that **target-time objective/stopping, rather than renderer capacity alone, is the immediate bottleneck**. However, the worst-tail pass margin is only about `0.034 dB`, and `k=27` was selected on this development cohort. Do not promote it to held-out efficacy yet, do not choose another development rule, and do not touch the official LOL-v2 Real test or cross-dataset sets.

---

# OPEN one-hour task — T062-C: fresh train-split qualification of frozen step-27 candidate

**Single hypothesis / engineering objective.** Test exactly one claim: the frozen T062-B candidate (`L_spa + 10 L_exp + 5 L_col`, exact T036 12-D action space, global stop `k=27`) retains its material gain and safety envelope on a fresh, previously unused LOL-v2 Real **training-split** cohort. This is an internal fresh qualification against development overfitting, not a final test or baseline claim.

**Fixed cohort and inputs/settings.** Build exactly one fresh 100-pair qualification cohort from LOL-v2 Real `Train` only. Exclude every low-image filename in `research_log/T036A_cohort/manifest.json`. From the remaining paired train images, compute `SHA256("T062C-v1:" + normalized_relative_low_path)` and take the 100 smallest hashes; sort the selected cohort by that hash and persist the manifest/hash **before any normal/reference image is opened**. Do not inspect image quality, labels, condition IDs, or prior outcome metrics when selecting the cohort.

Run three frozen target-free paths independently from each selected low image: (1) exact accepted T026-A; (2) exact accepted T036-A; (3) exact T062 candidate using the unchanged T062-A low-only gate, identity 12-D state, CommonRegion2/CommonBox, Adam `lr=0.03`, fixed three-term objective and **exactly 27 active updates**, then output step 27. Reuse accepted code/model/asset hashes; no weights, thresholds, objective terms, optimizer settings, action coordinates, gate logic, or stopping rule may change. Freeze and hash all 300 outputs plus configs/provenance before opening any of the 100 clean/normal references. Clean references may then be used only by a separate offline evaluator.

**Acceptance / stop criteria.** Classify `FRESH_QUALIFICATION_PASS` only if all hold on the fresh 100: (a) T062 mean PSNR delta versus T036 `>= +2.00 dB`; (b) median PSNR delta versus T036 `> 0`; (c) regressions versus T026-A `<=29/100`; (d) worst paired PSNR delta versus T026-A `>= -5.614 dB`; and (e) mean RGB-SSIM delta versus T036 `>= -0.001`. Otherwise classify `NEGATIVE` and stop the fixed-step-27 qualification route. Do not alter `k`, choose a second fresh cohort, or tune anything after seeing results.

**Explicit non-goals.** No use of the existing 100-image development references for any new choice; no alternative stop step or per-image stopping; no loss-weight/exposure-target/pooling/lr/step-budget/action-space sweep; no T014/T059/T060 hybrid; no source retraining; no SOTA/baseline sweep beyond the exact T026/T036 controls required for paired qualification; no official LOL-v2 Real test; no LSRW/UHD-LL/other cross-dataset access; no final Ours-vs-baseline gap claim. Never allow clean targets, PSNR/SSIM, baseline outcomes, or oracle quantities into adaptation or output selection. Never modify `coordination/CODEX_TO_CHATGPT.md` except by appending the completion report. Do not update `coordination/PROJECT_STATE.md` yourself.

**Expected evidence.** Commit the deterministic fresh-cohort selector/manifest and its SHA, exact exclusion proof for the original 100 development images, frozen code/model/asset/config bindings for all three paths, a global output-freeze receipt proving all 300 outputs were finalized before first normal/reference read, per-image paired PSNR/RGB-SSIM tables, all five acceptance gates, and an independent verifier that rechecks cohort selection, output hashes, metric arithmetic, and access ordering. Any cohort overlap with the original 100, provenance mismatch, clean/reference read before global output freeze, target-informed inference/selection, or official/cross-dataset access → `BLOCKED` and stop.